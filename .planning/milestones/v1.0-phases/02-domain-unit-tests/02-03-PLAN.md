---
phase: 02-domain-unit-tests
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [src/game/TeamData.test.js]
autonomous: true
requirements: [DOM-03]
must_haves:
  truths:
    - "TeamData.js roster model (assign/reset/availability) is tested and pass green"
    - "The domain suite runs with no mocks and no test-setup dependency"
  artifacts:
    - src/game/TeamData.test.js
  key_links:
    - "TeamData.test.js imports directly from @/game/TeamData.js, no mocking, no test/setup.js dependency"
---

<objective>
Create `src/game/TeamData.test.js` testing the roster model's documented happy-path behavior: `assignPlayer`, `resetPlayer`, `resetAllPlayers`, `getPlayersForPositions`, `availablePlayerIDs`, `allPlayers`, `teamName`, `getPlayerByID`.

Purpose: Lock down roster assignment/availability tracking as a regression baseline, satisfying DOM-03.
Output: New `src/game/TeamData.test.js` (all green, zero mocking).
</objective>

<execution_context>
@/mnt/c/Users/taragin/OneDrive - Hewlett Packard Enterprise/Config/.config/opencode/gsd-core/workflows/execute-plan.md
@/mnt/c/Users/taragin/OneDrive - Hewlett Packard Enterprise/Config/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-domain-unit-tests/02-CONTEXT.md
@src/game/TeamData.js
@src/game/playOutcome.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create TeamData.test.js with happy-path roster coverage</name>
  <files>src/game/TeamData.test.js</files>
  <read_first>
    src/game/TeamData.js (all 62 lines — constructor shape, all 8 public methods/properties)
    src/game/playOutcome.test.js (style reference: flat describe/it, `@/game/...` import pattern, no semicolons)
  </read_first>
  <behavior>
    Construct a fresh `new TeamData({ team: { name: 'Testers' }, players: { p1: { id: 'p1', position: 'QB' }, p2: { id: 'p2', position: 'RB' }, p3: { id: 'p3', position: 'RB' } } })` per test (fresh instance each test avoids cross-test state leakage since `availablePlayers` is instance mutable state).
    - Constructor: `resetAllPlayers()` runs automatically, so `availablePlayerIDs()` immediately after construction contains all of `p1`, `p2`, `p3` (order-independent — use `toEqual(expect.arrayContaining([...]))` or sort before compare).
    - `getPlayerByID('p1')` -> returns `{ id: 'p1', position: 'QB' }`.
    - `assignPlayer('p1')`: removes `p1` from availability — `availablePlayerIDs()` no longer contains `p1` afterward; return value of `assignPlayer` is `true` (Set.delete returns true when the element existed).
    - `resetPlayer('p1')` after an assign: `p1` reappears in `availablePlayerIDs()`.
    - `resetAllPlayers()`: after assigning multiple players, calling `resetAllPlayers()` restores all IDs to `availablePlayerIDs()`.
    - `getPlayersForPositions(['RB'])`: returns array of IDs whose position matches, e.g. `['p2', 'p3']` (order-independent compare) — verify it only returns currently-available players (assign `p2` first, then confirm `getPlayersForPositions(['RB'])` returns only `['p3']`).
    - `allPlayers()`: returns the full players object unchanged, e.g. `{ p1: {...}, p2: {...}, p3: {...} }`.
    - `teamName()`: returns `'Testers'` for the constructed team. Also test the fallback branch — construct a TeamData with `team: {}` (no `name` property) and confirm `teamName()` returns `'XYZ'` (this is a documented, in-contract fallback return value, not a defensive no-op — include it).
  </behavior>
  <action>
    Create src/game/TeamData.test.js importing `{ TeamData }` from `@/game/TeamData.js` and `{ describe, it, expect } from 'vitest'`. Build a small fixture players map (3 players: one QB, two RB) reused across tests, constructing a fresh `new TeamData(...)` in each `it` (or a `beforeEach` that reassigns) to avoid mutable-Set state bleeding between tests. Follow the flat `describe('TeamData', ...)` + nested per-method `describe` blocks matching playOutcome.test.js style. Do not test unknown player IDs, double-assign, or double-reset edge cases (D-06 happy-path only). No semicolons, single quotes, 2-space indent, no trailing commas.
  </action>
  <verify>
    <automated>npx vitest run src/game/TeamData.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/game/TeamData.test.js` exits 0, all tests pass
    - Tests cover `assignPlayer`, `resetPlayer`, `resetAllPlayers`, `getPlayersForPositions`, `availablePlayerIDs`, `allPlayers`, `teamName` (including the `'XYZ'` fallback), `getPlayerByID`
    - No test asserts behavior for an unknown/missing player ID, a double-assign, or a double-reset
    - No `vi.mock` or mocking utilities imported anywhere in the file
    - Each test constructs its own fresh `TeamData` instance (no shared mutable instance reused unreset across `it` blocks)
  </acceptance_criteria>
  <done>`npx vitest run src/game/TeamData.test.js` passes with happy-path coverage per behavior list, no mocking, no edge-case tests.</done>
</task>

</tasks>

## Artifacts this phase produces

- `src/game/TeamData.test.js` (new) — `describe('TeamData', ...)` with nested blocks per method: `getPlayerByID`, `assignPlayer`, `resetPlayer`, `resetAllPlayers`, `getPlayersForPositions`, `availablePlayerIDs`, `allPlayers`, `teamName` (incl. `'XYZ'` fallback).

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure unit tests of client-side domain logic only; no trust boundary crossed (no network, no user input, no persistence) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | N/A | TeamData.test.js | low | accept | Pure in-process unit test of dependency-free class; no attack surface introduced |
</threat_model>

<verification>
Run `npx vitest run src/game/TeamData.test.js` — all tests pass, exit code 0. Confirm no mocking and no shared mutable state across tests.
</verification>

<success_criteria>
`TeamData.js` roster model (assign/reset/availability) is tested and pass green, satisfying DOM-03 and ROADMAP Phase 2 success criterion 3.
</success_criteria>

<output>
Create `.planning/phases/02-domain-unit-tests/02-03-SUMMARY.md` when done
</output>
