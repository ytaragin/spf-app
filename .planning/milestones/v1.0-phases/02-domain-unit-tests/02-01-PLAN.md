---
phase: 02-domain-unit-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/game/playOutcome.test.js]
autonomous: true
requirements: [DOM-01]
must_haves:
  truths:
    - "playOutcome.js functions (classify, color, summary, turnover, possession logic) are tested and pass green"
    - "The domain suite runs with no mocks and no test-setup dependency"
  artifacts:
    - src/game/playOutcome.test.js
  key_links:
    - "playOutcome.test.js imports directly from @/game/playOutcome.js, no mocking, no test/setup.js dependency"
---

<objective>
Expand the existing `src/game/playOutcome.test.js` (created in Phase 1 with 2 proving tests) in place to cover the full `playOutcome.js` public API: `isTurnover`, `netYards`, `outcomeIcon`, `outcomeLabel`, `outcomeSummary`, `outcomeColor`, `classifyOutcome`, `managedTeamHadPossession`.

Purpose: Lock down the highest-value pure domain logic (perspective-based play classification) as a regression baseline, satisfying DOM-01.
Output: Updated `src/game/playOutcome.test.js` with nested `describe` blocks per function, all green, zero mocking.
</objective>

<execution_context>
@/mnt/c/Users/taragin/OneDrive - Hewlett Packard Enterprise/Config/.config/opencode/gsd-core/workflows/execute-plan.md
@/mnt/c/Users/taragin/OneDrive - Hewlett Packard Enterprise/Config/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-domain-unit-tests/02-CONTEXT.md
@src/game/playOutcome.js
@src/game/playOutcome.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand playOutcome.test.js with full function coverage</name>
  <files>src/game/playOutcome.test.js</files>
  <read_first>
    src/game/playOutcome.js (all 104 lines — every exported function and its JSDoc)
    src/game/playOutcome.test.js (existing 2 tests — do not delete or restructure them, add alongside)
  </read_first>
  <behavior>
    - Existing tests (do not remove): `outcomeColor` favorable positive-yardage completion returns 'success'; `isTurnover` recognizes `TurnOver` result_type.
    - `isTurnover`: returns false for a non-turnover result_type (e.g. `{ result_type: 'Complete' }`).
    - `netYards`: returns the numeric `result` value for `{ result: 8 }`; returns `0` for `{ result: 'abc' }` (non-numeric per D-03 happy-path scope — this IS the documented fallback behavior, test it since it's in the exported contract, not an edge-case defensive branch).
    - `outcomeIcon`: returns `'mdi-alert-octagon'` for a turnover result; returns `'mdi-arrow-up-bold'` for positive yardage non-turnover (e.g. `{ result_type: 'Complete', result: 5 }`); returns `'mdi-football'` for zero/negative yardage non-turnover (e.g. `{ result_type: 'Incomplete', result: 0 }`).
    - `outcomeLabel`: returns `'Turnover'` for a turnover result; returns the raw `result_type` for a non-turnover result (e.g. `{ result_type: 'Complete' }` -> `'Complete'`).
    - `outcomeSummary`: returns `'Turnover - {n} yards'` shape for a turnover with a yardage value (e.g. `{ result_type: 'TurnOver', result: -3 }` -> `'Turnover - -3 yards'`); returns `'{result_type} - {n} yards'` shape for a non-turnover (e.g. `{ result_type: 'Complete', result: 8 }` -> `'Complete - 8 yards'`).
    - `outcomeColor` D-02: test BOTH `favorable: true` (default, already partially covered) and `favorable: false` explicitly. Favorable case: positive-yardage non-turnover -> `'success'`; turnover -> `'error'`. Non-favorable (`favorable: false`, i.e. managed team was defending) case: positive-yardage non-turnover (bad for defense) -> `'error'`; a turnover (good for defense) -> `'success'`.
    - `classifyOutcome`: returns an object with all five keys (`color`, `icon`, `label`, `summary`, `isTurnover`, `netYards`) populated consistently for one representative result, e.g. `{ result_type: 'Complete', result: 8 }` produces `color: 'success'`, `icon: 'mdi-arrow-up-bold'`, `label: 'Complete'`, `summary: 'Complete - 8 yards'`, `isTurnover: false`, `netYards: 8`.
    - `managedTeamHadPossession` D-02: matching team case — `play.new_state.possession === 'Home'`, `managedTeam === 'Home'` -> returns `true`. Mismatched/inverted case — `possession === 'Away'`, `managedTeam === 'Home'` -> returns `false`.
  </behavior>
  <action>
    Add new `describe` blocks (one per function name) inside the existing top-level `describe('playOutcome', ...)` block in src/game/playOutcome.test.js, alongside the 2 existing `it` blocks (do not delete them — D-01). Import any additional named exports needed from `@/game/playOutcome.js` (isTurnover already imported; add netYards, outcomeIcon, outcomeLabel, outcomeSummary, outcomeColor, classifyOutcome, managedTeamHadPossession). Follow the existing flat `describe`/`it` style already in the file. Do not test the null/undefined-input defensive branches (D-03 — happy-path only for defensive-coding fallbacks like a null `result` object entirely, or `managedTeamHadPossession` with a null `play`/`managedTeam`); the non-numeric-`result`-string fallback in `netYards` IS in scope since it's directly documented in the JSDoc contract ("0 when absent/non-numeric") and is one of the behavior bullets above — include it as a single happy-path-style assertion, not an exhaustive defensive-branch sweep. No semicolons, single quotes, 2-space indent, no trailing commas (Prettier config).
  </action>
  <verify>
    <automated>npx vitest run src/game/playOutcome.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/game/playOutcome.test.js` exits 0, all tests pass
    - File still contains the original 2 `it` blocks ("colors a favorable positive-yardage completion as success", "recognizes a turnover result") unmodified
    - New `describe` blocks exist for: netYards, outcomeIcon, outcomeLabel, outcomeSummary, outcomeColor (with both favorable true/false cases), classifyOutcome, managedTeamHadPossession
    - No `vi.mock` or mocking utilities imported anywhere in the file
    - `outcomeColor` is tested with `favorable: false` at least once, asserting inversion (turnover -> 'success' when favorable is false)
  </acceptance_criteria>
  <done>`npx vitest run src/game/playOutcome.test.js` passes with all functions covered per behavior list, original 2 tests intact, no mocking.</done>
</task>

</tasks>

## Artifacts this phase produces

- `src/game/playOutcome.test.js` (expanded in place) — `describe('playOutcome', ...)` with nested blocks: existing `outcomeColor` favorable + `isTurnover` tests, plus new blocks for `netYards`, `outcomeIcon`, `outcomeLabel`, `outcomeSummary`, `outcomeColor` (favorable true/false), `classifyOutcome`, `managedTeamHadPossession`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure unit tests of client-side domain logic only; no trust boundary crossed (no network, no user input, no persistence) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | N/A | playOutcome.test.js | low | accept | Pure in-process unit test of dependency-free functions; no attack surface introduced |
</threat_model>

<verification>
Run `npx vitest run src/game/playOutcome.test.js` — all tests pass, exit code 0. Confirm the file has no `vi.mock`/`axios`/DOM imports.
</verification>

<success_criteria>
`playOutcome.js` functions (classify, color, summary, turnover, possession logic) are tested and pass green, satisfying DOM-01 and ROADMAP Phase 2 success criterion 1.
</success_criteria>

<output>
Create `.planning/phases/02-domain-unit-tests/02-01-SUMMARY.md` when done
</output>
