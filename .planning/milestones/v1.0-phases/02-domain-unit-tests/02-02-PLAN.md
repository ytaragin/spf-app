---
phase: 02-domain-unit-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/game/SPFMetadata.test.js, .planning/ISSUES.md]
autonomous: true
requirements: [DOM-02]
must_haves:
  truths:
    - "SPFMetadata.js box↔position mapping and label helpers are tested and pass green"
    - "The domain suite runs with no mocks and no test-setup dependency"
    - "The two known SPFMetadata bugs (D-07) are documented as tech debt, not fixed and not asserted as correct"
  artifacts:
    - src/game/SPFMetadata.test.js
    - .planning/ISSUES.md
  key_links:
    - "SPFMetadata.test.js imports directly from @/game/SPFMetadata.js, no mocking, no test/setup.js dependency"
    - "ISSUES.md documents the InsideRun/InsideRight duplicate code:'IR' bug and the getBoxLabel case-mismatch bug"
---

<objective>
Create `src/game/SPFMetadata.test.js` testing the box↔position mapping and label helper methods with representative sampling for simple getters (D-04) and full-coverage for the two "interesting" logic methods (D-05). Also create `.planning/ISSUES.md` documenting the two known bugs (D-07) as tech debt — flagged, not fixed, not tested as correct behavior.

Purpose: Lock down box/position/play metadata lookups as a regression baseline while explicitly not laundering known bugs into "expected" behavior, satisfying DOM-02.
Output: New `src/game/SPFMetadata.test.js` (all green, zero mocking) and new `.planning/ISSUES.md`.
</objective>

<execution_context>
@/mnt/c/Users/taragin/OneDrive - Hewlett Packard Enterprise/Config/.config/opencode/gsd-core/workflows/execute-plan.md
@/mnt/c/Users/taragin/OneDrive - Hewlett Packard Enterprise/Config/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-domain-unit-tests/02-CONTEXT.md
@src/game/SPFMetadata.js
@src/game/playOutcome.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SPFMetadata.test.js with sampled + full-coverage assertions</name>
  <files>src/game/SPFMetadata.test.js</files>
  <read_first>
    src/game/SPFMetadata.js (all 295 lines — constructor's shared-reference objects, all 15 public methods)
    src/game/playOutcome.test.js (style reference: flat describe/it, `@/game/...` import pattern, no semicolons)
  </read_first>
  <behavior>
    D-04 representative-sample getters (one instance per constructor call, e.g. `const meta = new SPFMetadata()`):
    - `getBoxLabel`: `getBoxLabel('box_a')` -> `'Box A'` (a working box_* label). Do NOT assert `getBoxLabel('le')` returns `'LE'` (D-07 bug — case mismatch, out of scope to assert as correct).
    - `getPositionForABox`: `getPositionForABox('box_a')` -> `['DL', 'LB']` (defRow1 sample); `getPositionForABox('box_f')` -> `['LB']` (defRow2 sample); `getPositionForABox('box_k')` -> `['DB']` (defRow3 sample); `getPositionForABox('box_l')` -> `['DB']` with `allowMultiple: true` present via `getPositionMetaData` (defRow3BoxL variant sample).
    - `getPositionMetaData`: `getPositionMetaData('box_a')` returns an object containing `positions: ['DL', 'LB']` and `allowMultiple: true`.
    - `getOffensivePlayNames`: returns an array containing all 9 keys of `offensivePlays` (InsideRun, InsideLeft, InsideRight, SweepLeft, EndAround, ScreenPass, QuickPass, ShortPass, LongPass) — assert length 9 and `toContain` a couple of names, not full deep-equal ordering dependency if fragile; array-based deep equal is fine if order is stable (object key order is stable in JS).
    - `getDefensivePlayNames`: returns array containing all 4 keys (RunDefense, PassDefense, PreventDefense, Blitz).
    - `getOffensePlayInfo`: `getOffensePlayInfo('InsideLeft')` -> object with `code: 'IL'`, `description` containing 'Inside Left', `boxes` array. Also test `getOffensePlayInfo(null)` -> `null` (documented guard, not a defensive edge case — it's an explicit `if (!play) return null` branch in the public contract).
    - `getDefensePlayInfo`: `getDefensePlayInfo('Blitz')` -> object with `code: 'Blitz'`, `description: 'Pass - Blitz Defense'`, `boxes` array containing 'le' and 're'.

    D-07 bug-safe assertions for `offensivePlays` (do NOT assert on `code` for these two):
    - `getOffensePlayInfo('InsideRun')` -> assert `description` is `'Running, Inside Right [IR]'` and `boxes` is `['b1', 'b2', 'b3']`. Do NOT assert `code`.
    - `getOffensePlayInfo('InsideRight')` -> assert `description` is `'Running, Sweep Right [IR]'` and `boxes` is `['b1', 'b2', 'b3']`. Do NOT assert `code`.

    D-05 full-coverage "interesting" logic methods:
    - `getRelatedPassDefenseBox`: box that guards another (a key in `passDefenseGuarding`, e.g. `'re'`) -> returns `'box_n'`. Box that IS guarded (a value in `passDefenseGuarding`, e.g. `'box_n'`) -> returns `'re'`. Box with no pass-defense relationship at all (e.g. `'box_a'`, not in `passDefenseGuarding` keys or values) -> returns `null`.
    - `getBoxLayoutForPlay`: `getBoxLayoutForPlay('kickoff')` -> `{ offense: [['k']], defense: [['kr']] }` (case-insensitive: also test `getBoxLayoutForPlay('KickOff')` or `'KICKOFF'` matches the same branch). Default branch: `getBoxLayoutForPlay('InsideRun')` (or any non-kickoff string) -> `{ offense: <offensiveBoxes 3x7 array>, defense: <defensiveBoxes 3x5 array> }` — assert shape via `offense` and `defense` keys present and `offense[0]` containing `'le'`.
  </behavior>
  <action>
    Create src/game/SPFMetadata.test.js importing `{ SPFMetadata }` from `@/game/SPFMetadata.js` and `{ describe, it, expect }` from `vitest`. Instantiate a fresh `new SPFMetadata()` per test or in a shared `const meta = new SPFMetadata()` at describe scope (instances are stateless/read-only, so sharing is safe — the constructor only builds the static metadata object). Organize as flat `describe('SPFMetadata', ...)` with nested `describe` blocks per method, matching the style of playOutcome.test.js. When asserting on the `positions` array contents (per CONTEXT.md specifics note), compare array values (e.g. `toEqual(['DL', 'LB'])`), not object identity — the underlying `defRow1`/`defRow2`/etc. objects are shared by reference across multiple box keys but tests must not rely on that internal identity. No semicolons, single quotes, 2-space indent, no trailing commas.
  </action>
  <verify>
    <automated>npx vitest run src/game/SPFMetadata.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/game/SPFMetadata.test.js` exits 0, all tests pass
    - File does NOT assert `getBoxLabel('le')` equals `'LE'`
    - File does NOT assert a `code` value for `getOffensePlayInfo('InsideRun')` or `getOffensePlayInfo('InsideRight')`
    - `getRelatedPassDefenseBox` is tested in both directions plus the null-result case
    - `getBoxLayoutForPlay` is tested for both the `'kickoff'` branch (case-insensitive) and the default branch
    - No `vi.mock` or mocking utilities imported anywhere in the file
  </acceptance_criteria>
  <done>`npx vitest run src/game/SPFMetadata.test.js` passes with sampled + full-coverage assertions per behavior list, no bug-laundering assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Document known SPFMetadata bugs as tech debt in ISSUES.md</name>
  <files>.planning/ISSUES.md</files>
  <read_first>
    .planning/phases/02-domain-unit-tests/02-CONTEXT.md (D-07 section — exact bug descriptions)
    src/game/SPFMetadata.js (lines 89-108 labels object, lines 109-155 offensivePlays object — cite exact line context)
  </read_first>
  <action>
    Create .planning/ISSUES.md (new file, project has none yet) with a top-level `# Known Issues / Tech Debt` heading and a table or list with two entries: (1) `InsideRun` and `InsideRight` in `src/game/SPFMetadata.js` `offensivePlays` both have `code: 'IR'` — likely copy-paste bug, `InsideRight` should probably be a distinct code (e.g. `'SR'` for Sweep Right, matching its description "Sweep Right [IR]" which itself has a mismatched bracket-code); (2) `getBoxLabel(box)` in `src/game/SPFMetadata.js` never matches lowercase offensive-box keys (`'le'`, `'k'`, `'kr'`) against the `labels` object's uppercase keys (`LE`, `K`, `KR`) — falls through to returning the raw key unchanged. For each entry: state file + approximate line numbers, current behavior, why it's a bug, and explicit note "Not fixed in this milestone — source refactor is out of scope per PROJECT.md constraints; flagged here per Phase 2 CONTEXT.md D-07." Do not modify src/game/SPFMetadata.js.
  </action>
  <verify>
    <automated>test -f .planning/ISSUES.md && grep -c 'InsideRun' .planning/ISSUES.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ISSUES.md` exists
    - Contains a reference to both `InsideRun`/`InsideRight` duplicate `code: 'IR'` and `getBoxLabel` case-mismatch
    - States explicitly that neither bug is fixed in this milestone
    - `src/game/SPFMetadata.js` is unmodified (git diff shows no changes to that file from this task)
  </acceptance_criteria>
  <done>.planning/ISSUES.md exists, documents both D-07 bugs with explicit "not fixed" note, source file untouched.</done>
</task>

</tasks>

## Artifacts this phase produces

- `src/game/SPFMetadata.test.js` (new) — `describe('SPFMetadata', ...)` with nested blocks per method: `getBoxLabel`, `getPositionForABox`, `getPositionMetaData`, `getOffensivePlayNames`, `getDefensivePlayNames`, `getOffensePlayInfo`, `getDefensePlayInfo`, `getRelatedPassDefenseBox`, `getBoxLayoutForPlay`.
- `.planning/ISSUES.md` (new) — tech-debt entries for the `InsideRun`/`InsideRight` duplicate `code: 'IR'` bug and the `getBoxLabel` case-mismatch bug.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Pure unit tests of client-side domain logic only; no trust boundary crossed (no network, no user input, no persistence) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02 | N/A | SPFMetadata.test.js | low | accept | Pure in-process unit test of dependency-free class; no attack surface introduced |
| T-02-03 | Repudiation | ISSUES.md documentation | low | accept | Documentation-only artifact; no runtime behavior change, no security implication |
</threat_model>

<verification>
Run `npx vitest run src/game/SPFMetadata.test.js` — all tests pass, exit code 0. Confirm `.planning/ISSUES.md` exists and documents both D-07 bugs without modifying source.
</verification>

<success_criteria>
`SPFMetadata.js` box↔position mapping and label helpers are tested and pass green, satisfying DOM-02 and ROADMAP Phase 2 success criterion 2. Known bugs are documented, not fixed, not laundered into test assertions.
</success_criteria>

<output>
Create `.planning/phases/02-domain-unit-tests/02-02-SUMMARY.md` when done
</output>
