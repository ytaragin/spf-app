---
phase: 01-test-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vite.config.js
  - test/setup.js
  - src/game/playOutcome.test.js
autonomous: true
requirements: [FND-01, FND-02, FND-03, FND-04, FND-05]
must_haves:
  truths:
    - "npm run build succeeds on upgraded Vite 6 with no src refactor (FND-01)"
    - "npm test runs Vitest, resolves @->src, jsdom env + globals, one passing domain test (FND-02)"
    - "npm run test:coverage produces a report-only V8 summary over src/** (FND-04)"
    - "test/setup.js registers createVuetify factory + 3 jsdom shims and loads without error (FND-03)"
    - "npm scripts exist for run-once, watch, coverage, and E2E entry (FND-05)"
  artifacts:
    - vite.config.js
    - test/setup.js
    - src/game/playOutcome.test.js
    - package.json
  key_links:
    - "vite.config.js resolve.alias @->src reused by Vitest test block (no duplication, D-01)"
    - "src/game/playOutcome.test.js imports via @/game/playOutcome.js — proves alias resolves"
    - "vite.config.js test.setupFiles -> ./test/setup.js"
---

<objective>
Upgrade the minimal Vite 4 config to Vite 6 (matching Vue plugin majors), layer an
idiomatic Vitest runner on top via a merged `test` block, register jsdom shims + a
Vuetify factory in `test/setup.js`, enable report-only V8 coverage, add the four npm
scripts, and prove the whole toolchain with one real passing test against
`src/game/playOutcome.js`.

Purpose: Establish the test infrastructure (Wave 0 for the entire project) so Phases
2–5 can write domain, store, component, and E2E suites against a proven runner.
Output: Upgraded `package.json`/lockfile, merged `vite.config.js`, new `test/setup.js`,
new `src/game/playOutcome.test.js`, and a green `npm test`.
</objective>

<execution_context>
@/mnt/c/Users/taragin/OneDrive - Hewlett Packard Enterprise/Config/.config/opencode/gsd-core/workflows/execute-plan.md
@/mnt/c/Users/taragin/OneDrive - Hewlett Packard Enterprise/Config/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-test-foundation/01-RESEARCH.md
@.planning/phases/01-test-foundation/01-PATTERNS.md
@.planning/phases/01-test-foundation/01-CONTEXT.md
@vite.config.js
@package.json
@src/game/playOutcome.js
@src/main.js
</context>

## Phase Goal

**As a** developer, **I want to** run a working Vitest suite (`npm test`) against the app's real code through the full upgraded Vite 6 → Vitest → jsdom → `@` alias toolchain, **so that** I can add trustworthy tests in every later phase with confidence the runner works end to end.

## Vertical Slice Rationale

This test-infrastructure phase's "vertical slice" is the thinnest end-to-end proof that
`npm test` runs one real passing test through the entire toolchain. The tasks below are
strictly incremental — after each task the developer can do something they could not do
before:

- After Task 1: the app builds/runs on Vite 6 with the test tooling installed.
- After Task 2: `npm test` runs a real passing test resolving `@`→`src` under jsdom.
- After Task 3: coverage reports, all four npm scripts exist, and the Vuetify factory
  is ready for Phase 4 component mounts.

<artifacts_this_phase_produces>
### New devDependencies (package.json)
- `vitest@^3.2.7`
- `@vitest/coverage-v8@^3.2.7`
- `jsdom@^26.1.0`
- `@vue/test-utils@^2.4.11`

### Bumped devDependencies (package.json)
- `vite` `^4.4.6` → `^6.4.3`
- `@vitejs/plugin-vue` `^4.2.3` → `^5.2.4`
- `@vitejs/plugin-vue-jsx` `^3.0.1` → `^4.2.0`

### New npm scripts (package.json)
- `test` → `vitest run`
- `test:watch` → `vitest`
- `test:coverage` → `vitest run --coverage`
- `test:e2e` → `playwright test` (entry-script placeholder; Playwright NOT installed this phase)

### New vite.config.js keys
- Top-of-file directive: `/// <reference types="vitest/config" />`
- `test.environment` = `'jsdom'`
- `test.globals` = `true`
- `test.setupFiles` = `['./test/setup.js']`
- `test.include` = `['src/**/*.{test,spec}.{js,jsx}']`
- `test.coverage` = `{ provider: 'v8', all: true, reporter: ['text', 'html'], include: ['src/**'], exclude: [...] }`

### New files
- `test/setup.js` — exports `createTestVuetify()`; registers `ResizeObserver`, `window.matchMedia`, `CSS.supports` shims
- `src/game/playOutcome.test.js` — proving test importing `@/game/playOutcome.js`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade to Vite 6, install test tooling, add npm scripts</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (the file being modified — current scripts + devDependencies)
    - .planning/phases/01-test-foundation/01-PATTERNS.md (§package.json — exact bump/add list + scripts block)
    - .planning/phases/01-test-foundation/01-RESEARCH.md (§Standard Stack, §Installation, Pitfall 1)
  </read_first>
  <action>
    Bump devDependencies per D-04 + FND-01: `vite` to `^6.4.3`, `@vitejs/plugin-vue` to
    `^5.2.4`, `@vitejs/plugin-vue-jsx` to `^4.2.0`. Add devDependencies:
    `vitest@^3.2.7`, `@vitest/coverage-v8@^3.2.7`, `jsdom@^26.1.0`,
    `@vue/test-utils@^2.4.11`. Do NOT bump to Vite 7/8 or Vitest 4 — the registry lists
    them as latest but Vitest 4 requires Vite 7+ and breaks the D-04 Vite-6 plugin lock;
    Vitest 3.2.7 + Vite 6 is the only self-consistent pair (RESEARCH A2). `@vitest/coverage-v8`
    MUST match `vitest` exactly at `3.2.7` (Pitfall 1). Run the two `npm install -D`
    commands from RESEARCH §Installation to update the lockfile (do NOT hand-edit
    package-lock.json). Add four scripts to the `scripts` block: `test` = `vitest run`,
    `test:watch` = `vitest`, `test:coverage` = `vitest run --coverage`,
    `test:e2e` = `playwright test`. The `test:e2e` entry is a placeholder (FND-05, RESEARCH A3)
    — Playwright is NOT installed this phase; the script only needs to exist. Preserve JSON
    formatting style of the existing file. Do NOT touch `dependencies` or refactor any `src/`.
    After install, run `npm run build` to confirm the Vite 6 upgrade builds with no source
    change (FND-01).
  </action>
  <verify>
    <automated>npm run build</automated>
    <human-check>Run `npm run dev`, open the printed URL, confirm the app renders with no console errors (FND-01 dev-serve half — see 01-VALIDATION.md Manual-Only Verifications).</human-check>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 and produces a `dist/` bundle.
    - `package.json` devDependencies show `vite@^6.4.3`, `@vitejs/plugin-vue@^5.2.4`, `@vitejs/plugin-vue-jsx@^4.2.0`, and new `vitest@^3.2.7`, `@vitest/coverage-v8@^3.2.7`, `jsdom@^26.1.0`, `@vue/test-utils@^2.4.11`.
    - `package.json` scripts contains `test`, `test:watch`, `test:coverage`, `test:e2e` with the exact commands above.
    - `package-lock.json` is regenerated (installed by npm, not hand-edited) and committed.
    - No file under `src/` is modified.
  </acceptance_criteria>
  <done>Vite 6 upgrade + test tooling installed, four npm scripts present, `npm run build` green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Merge Vitest test block into vite.config.js + write proving test</name>
  <files>vite.config.js, src/game/playOutcome.test.js</files>
  <read_first>
    - vite.config.js (the file being modified — current 18-line form)
    - src/game/playOutcome.js (the test target — verified exports: outcomeColor, isTurnover)
    - .planning/phases/01-test-foundation/01-PATTERNS.md (§vite.config.js changes + §playOutcome.test.js verified assertions)
    - .planning/phases/01-test-foundation/01-RESEARCH.md (§Pattern 1, §Pattern 2, Pitfall 3, Pitfall 4)
  </read_first>
  <behavior>
    - Test 1: `outcomeColor({ result_type: 'Complete', result: 8 })` returns `'success'` (positive yardage, non-turnover, favorable defaults true).
    - Test 2: `isTurnover({ result_type: 'TurnOver' })` returns `true`.
    - The test imports both functions via the `@/game/playOutcome.js` alias path — a failing/unresolved alias must fail the run, proving FND-02.
  </behavior>
  <action>
    Per D-01, modify (do NOT replace) `vite.config.js`: add the directive
    `/// <reference types="vitest/config" />` as the FIRST line, above the imports. Keep
    `import { defineConfig } from 'vite'` — do NOT switch the import source to `vitest/config`
    (Pitfall 3). Leave the existing `plugins` and the `@`→`src` `resolve.alias` untouched —
    the alias is reused by Vitest with zero duplication (FND-02, D-01). Add a `test` key to
    the `defineConfig` object with: `environment: 'jsdom'`, `globals: true` (D-02),
    `setupFiles: ['./test/setup.js']`, `include: ['src/**/*.{test,spec}.{js,jsx}']` (D-03
    co-located tests). The coverage sub-block is added in this same file but its behavior is
    exercised in Task 3; include it now: `coverage: { provider: 'v8', all: true,
    reporter: ['text', 'html'], include: ['src/**'], exclude: ['src/main.js',
    'src/stores/counter.js', 'src/**/*.test.{js,jsx}', 'test/**'] }` with NO thresholds
    (COV-01 deferred, FND-04). Create `src/game/playOutcome.test.js` co-located with source
    (D-03): explicitly import `describe, it, expect` from `vitest` (lint-clean even with
    globals) and import `outcomeColor, isTurnover` from `@/game/playOutcome.js`; assert the
    two behaviors above. Follow project style throughout: no semicolons, single quotes,
    2-space indent, print width 100, no trailing commas. NOTE: this task depends on
    `test/setup.js` existing at run time — create it in Task 3 before running `npm test`,
    OR create the stub setup file here if you run the test in this task; the plan orders
    setup fully in Task 3, so run the proving test after Task 3.
  </action>
  <verify>
    <automated>node -e "const c=require('fs').readFileSync('vite.config.js','utf8'); if(!/vitest\/config/.test(c)||!/environment:\s*'jsdom'/.test(c)||!/globals:\s*true/.test(c)) process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `vite.config.js` first line is `/// <reference types="vitest/config" />`.
    - `vite.config.js` still imports `defineConfig` from `vite` (not `vitest/config`) and retains the original `@`→`src` alias unchanged.
    - `vite.config.js` `test` block contains `environment: 'jsdom'`, `globals: true`, `setupFiles: ['./test/setup.js']`, `include: ['src/**/*.{test,spec}.{js,jsx}']`, and the `coverage` block with `provider: 'v8'`, `all: true`, and the four exclude entries; no coverage `thresholds` key exists.
    - `src/game/playOutcome.test.js` exists, imports from `@/game/playOutcome.js`, and asserts the two behaviors above.
    - New/modified files follow project style (no semicolons, single quotes, 2-space indent, no trailing commas).
  </acceptance_criteria>
  <done>Merged Vitest config in place with reused alias; proving test authored against real playOutcome functions.</done>
</task>

<task type="auto">
  <name>Task 3: Write test/setup.js (jsdom shims + Vuetify factory) and prove the suite green</name>
  <files>test/setup.js</files>
  <read_first>
    - src/main.js (createVuetify registration reference — mirror the import shape, NOT the theme/blueprint/icons config)
    - .planning/phases/01-test-foundation/01-PATTERNS.md (§test/setup.js full content + Vuetify registration analog)
    - .planning/phases/01-test-foundation/01-RESEARCH.md (§Pattern 3, Pitfall 2)
  </read_first>
  <action>
    Create the top-level `test/` directory and `test/setup.js` (D-03 shared infra location).
    Register the three jsdom shims Vuetify assumes exist but jsdom omits (Pitfall 2, FND-03):
    `globalThis.ResizeObserver` as a class with no-op `observe`/`unobserve`/`disconnect`;
    a `window.matchMedia` polyfill (guarded by `if (!window.matchMedia)`) returning an object
    with `matches: false`, the query echoed in `media`, and no-op `addListener`/`removeListener`/
    `addEventListener`/`removeEventListener`/`dispatchEvent`; and `CSS.supports` (guard
    `globalThis.CSS` then `CSS.supports`) returning `false`. Export a `createTestVuetify()`
    factory that returns `createVuetify({ components, directives })` using `import * as
    components from 'vuetify/components'` and `import * as directives from 'vuetify/directives'`
    — mirror the import shape from `src/main.js` but do NOT copy the theme/blueprint/mdi
    config and do NOT call `app.use()` globally (Vuetify is a per-mount plugin; the factory
    is consumed per-mount in Phase 4). Follow project style: no semicolons, single quotes,
    2-space indent, no trailing commas. Then run `npm test` to prove the full slice green:
    setupFiles loads without error, the `@` alias resolves, jsdom + globals work, and the
    proving test from Task 2 passes. Also run `npm run test:coverage` to confirm the
    report-only V8 summary generates over `src/**` (FND-04).
  </action>
  <verify>
    <automated>npm test && npm run test:coverage</automated>
  </verify>
  <acceptance_criteria>
    - `test/setup.js` exists and exports `createTestVuetify`.
    - `test/setup.js` defines `globalThis.ResizeObserver`, a guarded `window.matchMedia`, and a guarded `CSS.supports` shim.
    - `npm test` exits 0 with the two proving-test assertions passing; no `ResizeObserver`/`matchMedia`/`CSS.supports is not a function` errors from setup.
    - `npm run test:coverage` exits 0 and prints a text coverage summary that includes `src/` files (report-only, no threshold failure).
    - `test/setup.js` follows project style.
  </acceptance_criteria>
  <done>Shared setup registers shims + Vuetify factory; `npm test` and `npm run test:coverage` both green — the full toolchain slice is proven end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local devDependencies | Package tarballs installed into the dev/test toolchain (no runtime/user-input surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | npm devDependency installs (vite/vitest/plugins/jsdom/test-utils) | high | mitigate | RESEARCH §Package Legitimacy Audit verified all packages against official `vitejs`/`vitest-dev`/`jsdom`/`vuejs` repos; no `postinstall`; all `[OK]`/approved (seam SUS is an environment-clock false positive). No `[ASSUMED]`/`[SUS]`/`[SLOP]` requiring a human-verify checkpoint. |
| T-01-02 | Tampering | package-lock.json regeneration | medium | mitigate | Install via `npm install -D` (not hand-edit); commit the regenerated lockfile and review the diff for unexpected transitive deps. |

> Security gate: PASS (tooling-only phase, no runtime/auth/network/user-input surface — RESEARCH §Security Domain). No legitimacy checkpoint required.
</threat_model>

<verification>
- `npm run build` succeeds on Vite 6 with no `src/` change (FND-01 build half).
- `npm run dev` renders the app in a browser with no console errors (FND-01 dev half — manual, see 01-VALIDATION.md).
- `npm test` runs Vitest, resolves `@`→`src`, uses jsdom + globals, one passing domain test (FND-02).
- `test/setup.js` loads without error and registers the three shims + Vuetify factory (FND-03).
- `npm run test:coverage` produces a report-only V8 summary over `src/**`, no threshold (FND-04).
- `npm run` lists `test`, `test:watch`, `test:coverage`, `test:e2e` (FND-05).
</verification>

<success_criteria>
- All five ROADMAP Phase 1 success criteria are TRUE.
- `npm test` is green with the proving test passing through the full Vite 6 → Vitest → jsdom → `@` alias slice.
- No `src/` source file (other than the new co-located test) was refactored.
</success_criteria>

<output>
Create `.planning/phases/01-test-foundation/01-01-SUMMARY.md` when done.
</output>
