---
phase: 06-pure-foundations-ws-url-envelope-parsing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game/gameSocketUrl.js
  - src/game/gameSocketUrl.test.js
autonomous: true
requirements: [WS-01]

must_haves:
  truths:
    - "toWebSocketUrl('http://host:8000') derives a ws:// URL (D-04)"
    - "toWebSocketUrl('https://host') derives a wss:// URL (D-04)"
    - "The derived URL preserves the original host and port (D-04)"
    - "The derived URL pathname is /game/ws, replacing any existing path (D-04)"
    - "Trailing slashes in the base URL are handled naturally via the URL constructor (D-04)"
    - "toWebSocketUrl throws on an invalid, empty, or unset base URL — fail loud, never returns null (D-05)"
    - "No new env var is introduced; the caller passes VITE_API_BASE_URL (the same value stores read)"
  artifacts:
    - src/game/gameSocketUrl.js
    - src/game/gameSocketUrl.test.js
  key_links:
    - "toWebSocketUrl consumes the same VITE_API_BASE_URL value gameStore reads (src/stores/gameStore.js:51)"
---

<objective>
Deliver a pure, Vue-free `toWebSocketUrl(baseUrl)` that derives the realtime WebSocket URL from the REST base URL, implementing WS-01.

Purpose: Establishes the WS wire address contract as a zero-dependency pure module, consumed later by the Phase 9 `useGameSocket` composable. No new env var — reuses `VITE_API_BASE_URL`.
Output: `src/game/gameSocketUrl.js` (exports `toWebSocketUrl`) + colocated zero-mock `src/game/gameSocketUrl.test.js`.
</objective>

<execution_context>
@/mnt/c/Users/taragin/OneDrive - Hewlett Packard Enterprise/Config/.config/opencode/gsd-core/workflows/execute-plan.md
@/mnt/c/Users/taragin/OneDrive - Hewlett Packard Enterprise/Config/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-pure-foundations-ws-url-envelope-parsing/06-CONTEXT.md
@src/game/playOutcome.js
@src/game/playOutcome.test.js
</context>

## Artifacts this phase produces

- `toWebSocketUrl` — new pure function in new file `src/game/gameSocketUrl.js`
- `src/game/gameSocketUrl.test.js` — new colocated zero-mock Vitest suite

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement toWebSocketUrl pure module</name>
  <files>src/game/gameSocketUrl.js, src/game/gameSocketUrl.test.js</files>
  <read_first>
    - src/game/playOutcome.js — mirror the module header comment ("Pure functions only — no store access"), JSDoc-on-export style, no-semicolons/single-quote/2-space Prettier formatting.
    - src/game/playOutcome.test.js — mirror the zero-mock Vitest style: `import { describe, it, expect } from 'vitest'`, nested `describe` per behavior, plain assertions, no jsdom/Vuetify/Pinia setup.
    - src/stores/gameStore.js line ~51 — the existing `import.meta.env.VITE_API_BASE_URL` read; toWebSocketUrl derives from this same value (do NOT introduce a new env var).
    - AGENTS.md — Prettier config (no semicolons, single quotes, 2-space, 100-col) and the `console.error` interpolated-context logging convention (not used in this module, but confirms house style).
  </read_first>
  <behavior>
    - Test: toWebSocketUrl('http://localhost:8000') returns a string starting 'ws://localhost:8000' with pathname '/game/ws'.
    - Test: toWebSocketUrl('https://api.example.com') returns 'wss://api.example.com/game/ws'.
    - Test: host AND port preserved — toWebSocketUrl('http://host:1234/some/existing/path') yields 'ws://host:1234/game/ws' (existing path replaced).
    - Test: trailing slash tolerated — toWebSocketUrl('http://host:8000/') yields 'ws://host:8000/game/ws'.
    - Test: throws on '' (empty), on a non-URL string like 'not a url', and on undefined/null input (D-05, fail loud) — assert via expect(() => toWebSocketUrl(x)).toThrow().
  </behavior>
  <action>
    Create `src/game/gameSocketUrl.js` exporting a single pure function `toWebSocketUrl(baseUrl)` with a JSDoc block (per playOutcome.js style) and a module-header comment noting "Pure — no Vue/store access". Use the platform `URL` constructor per D-04: construct `new URL(baseUrl)`; map protocol `http:`→`ws:` and `https:`→`wss:` by assigning `url.protocol`; leave host and port untouched (the URL object preserves them); set `url.pathname = '/game/ws'` (this replaces any existing path and normalizes trailing slashes automatically). Return `url.toString()` (or `url.href`). Per D-05, fail loud on bad input: if `baseUrl` is falsy/empty, or if `new URL(...)` throws, or the resulting protocol is not http/https, throw an Error with an interpolated context message (e.g. describing the invalid base URL) — NEVER return null and NEVER silently default. Do NOT read `import.meta.env` inside this module; the base URL is passed in by the caller. Colocate the zero-mock test file mirroring playOutcome.test.js exactly (no mocks, no jsdom).
  </action>
  <verify>
    <automated>npx vitest run src/game/gameSocketUrl.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Source: `src/game/gameSocketUrl.js` exists and exports `toWebSocketUrl`.
    - Source: module contains no `import` of Vue, Pinia, axios, or any store (pure module).
    - Behavior: toWebSocketUrl('http://h:8000') === 'ws://h:8000/game/ws'.
    - Behavior: toWebSocketUrl('https://h') === 'wss://h/game/ws'.
    - Behavior: toWebSocketUrl('http://h:8000/x/y/') === 'ws://h:8000/game/ws' (path replaced, trailing slash handled).
    - Behavior: toWebSocketUrl(''), toWebSocketUrl('not a url'), and toWebSocketUrl(undefined) each throw.
    - Test: `npx vitest run src/game/gameSocketUrl.test.js` exits 0.
  </acceptance_criteria>
  <done>toWebSocketUrl derives ws/wss URLs with /game/ws, preserves host+port, handles trailing slashes, throws on invalid input; colocated zero-mock tests pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/game/gameSocketUrl.test.js` exits 0.
- `npm run lint` passes for the new files (no Prettier/ESLint violations).
- Grep confirms no store/Vue/axios imports in `src/game/gameSocketUrl.js`.
</verification>

<success_criteria>
toWebSocketUrl satisfies all WS-01 success criteria: protocol swap (http→ws, https→wss), host+port preserved, `/game/ws` path appended (replacing existing path), trailing-slash tolerant, throws on invalid/empty/unset input, and introduces no new env var. Zero-mock unit tests cover all five behaviors above and pass.
</success_criteria>

<output>
Create `.planning/phases/06-pure-foundations-ws-url-envelope-parsing/06-01-SUMMARY.md` when done.
</output>
