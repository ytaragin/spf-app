---
phase: 06-pure-foundations-ws-url-envelope-parsing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/game/gameEvents.js
  - src/game/gameEvents.test.js
autonomous: true
requirements: [EVT-01]

must_haves:
  truths:
    - "parseEvent accepts an already-parsed object only; it does NOT JSON.parse (D-03)"
    - "On a valid known-variant envelope, parseEvent returns the envelope pass-through unchanged as { event, data } with data untouched — no rename to type/payload (D-01)"
    - "parseEvent accepts exactly the 5 known variants: GameStarted, OffensiveLineupSet, DefensiveLineupSet, NextPlayTypeSet, PlayRun (EVT-02)"
    - "On a malformed envelope (null/non-object input, missing/non-string event, missing data) parseEvent returns null (D-02)"
    - "On an unknown-variant event tag, parseEvent returns null (D-02)"
    - "parseEvent NEVER throws for any input"
    - "On rejection, parseEvent logs via console.error with a leading context phrase plus the offending event tag or a short snippet — NOT the full raw payload (D-06)"
  artifacts:
    - src/game/gameEvents.js
    - src/game/gameEvents.test.js
  key_links:
    - "parseEvent's { event, data } output is the input contract for Phase 8 dispatch glue; its null return is the ignore signal for Phase 9's onmessage handler"
---

<objective>
Deliver a pure, Vue-free `gameEvents.parseEvent(raw)` that validates and unwraps the tagged `{ event, data }` WebSocket envelope for the 5 known variants, rejecting malformed/unknown envelopes to `null` without throwing — implementing EVT-01.

Purpose: Establishes the envelope shape + variant validation contract as a zero-dependency pure module. Callers (Phase 8/9) use the defensive-null pattern.
Output: `src/game/gameEvents.js` (exports `parseEvent`) + colocated zero-mock `src/game/gameEvents.test.js`.
</objective>

<execution_context>
@/mnt/c/Users/taragin/OneDrive - Hewlett Packard Enterprise/Config/.config/opencode/gsd-core/workflows/execute-plan.md
@/mnt/c/Users/taragin/OneDrive - Hewlett Packard Enterprise/Config/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-pure-foundations-ws-url-envelope-parsing/06-CONTEXT.md
@src/game/playOutcome.js
@src/game/playOutcome.test.js
</context>

## Artifacts this phase produces

- `parseEvent` — new pure function in new file `src/game/gameEvents.js` (the `gameEvents` module)
- `src/game/gameEvents.test.js` — new colocated zero-mock Vitest suite

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement gameEvents.parseEvent pure module</name>
  <files>src/game/gameEvents.js, src/game/gameEvents.test.js</files>
  <read_first>
    - src/game/playOutcome.js — mirror the module header comment, JSDoc-on-export style, defensive optional-chaining / null-default approach (never throws), and the module-level UPPER_SNAKE_CASE constant pattern (e.g. TURNOVER_TYPE) for the known-variant set.
    - src/game/playOutcome.test.js — mirror the zero-mock Vitest style: `import { describe, it, expect } from 'vitest'`, nested `describe` per behavior, plain assertions, no jsdom/Vuetify/Pinia.
    - src/stores/gameStore.js lines ~273-297 and ~352 — the existing play_counter / new_state handling and PlayRun payload shape; parseEvent passes `data` through UNTOUCHED (does not inspect or reshape new_state).
    - AGENTS.md — the `console.error` interpolated-context logging convention (D-06 follows it) and Prettier config.
  </read_first>
  <behavior>
    - Test: parseEvent({ event: 'PlayRun', data: { new_state: { play_counter: 3 } } }) returns the SAME object shape { event: 'PlayRun', data: {...} } with data unchanged (use toEqual; assert no type/payload keys).
    - Test: each of the 5 known variants (GameStarted, OffensiveLineupSet, DefensiveLineupSet, NextPlayTypeSet, PlayRun) with a { data: {} } passes through.
    - Test: parseEvent({ event: 'Nope', data: {} }) returns null AND console.error was called (spy via vi.spyOn(console,'error')).
    - Test: malformed inputs all return null and never throw — null, undefined, 42, 'string', {} (missing event), { event: 123, data: {} } (non-string event), { event: 'PlayRun' } (missing data).
    - Test: console.error message includes the offending event tag (e.g. 'Nope') but assert it does NOT include a stringified full payload.
  </behavior>
  <action>
    Create `src/game/gameEvents.js` exporting a pure function `parseEvent(raw)` with a JSDoc block and a module-header comment ("Pure — no Vue/store access; validates WS envelope shape only"). Define the known-variant set as a module-level constant containing exactly: GameStarted, OffensiveLineupSet, DefensiveLineupSet, NextPlayTypeSet, PlayRun (representation — Set/array/object — is discretionary). Per D-03, treat `raw` as an already-parsed object; do NOT call JSON.parse. Validate shape defensively (never throw, D-02): reject when `raw` is null / not an object, when `raw.event` is missing or not a string, when `raw.data` is missing, or when `raw.event` is not in the known-variant set. On any rejection, call `console.error` with a leading context phrase and the offending `event` tag (or a short snippet when the tag is unavailable) — do NOT stringify or dump the full raw payload (D-06) — then return `null` (D-02). On success, return the envelope pass-through unchanged as `{ event: raw.event, data: raw.data }` (or return `raw` directly) — keep wire naming, do NOT rename to `{ type, payload }` and do NOT reshape `data` (D-01). Note for comment-text discipline: do not embed the literal rejection-log string that a test negative-greps for. Colocate the zero-mock test file mirroring playOutcome.test.js; use `vi.spyOn(console, 'error')` (import `vi` from vitest) to assert logging without any DOM/store mocks.
  </action>
  <verify>
    <automated>npx vitest run src/game/gameEvents.test.js</automated>
  </verify>
  <acceptance_criteria>
    - Source: `src/game/gameEvents.js` exists and exports `parseEvent`.
    - Source: module contains no import of Vue, Pinia, axios, or any store; contains no `JSON.parse`.
    - Behavior: parseEvent({event:'PlayRun',data:{x:1}}) deep-equals {event:'PlayRun',data:{x:1}} and has no `type`/`payload` keys.
    - Behavior: all 5 known variants with a data object return non-null pass-through.
    - Behavior: parseEvent({event:'Nope',data:{}}) returns null and console.error is called with a message containing 'Nope'.
    - Behavior: parseEvent(null), parseEvent(undefined), parseEvent(42), parseEvent({}), parseEvent({event:123,data:{}}), parseEvent({event:'PlayRun'}) each return null and none throw.
    - Test: `npx vitest run src/game/gameEvents.test.js` exits 0.
  </acceptance_criteria>
  <done>parseEvent validates the 5-variant envelope, passes valid ones through unchanged, returns null + console.error on malformed/unknown without ever throwing or dumping full payloads; colocated zero-mock tests pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/game/gameEvents.test.js` exits 0.
- `npm run lint` passes for the new files.
- Grep confirms no store/Vue/axios imports and no `JSON.parse` in `src/game/gameEvents.js`.
</verification>

<success_criteria>
parseEvent satisfies all EVT-01 success criteria: unwraps the `{ event, data }` envelope for the 5 known variants (pass-through unchanged), rejects malformed/unknown envelopes to `null` with a `console.error` log (context phrase + event tag, no full-payload dump), never throws, and never JSON.parses. Zero-mock unit tests cover valid variants, unknown variant, and all malformed-shape cases, and pass.
</success_criteria>

<output>
Create `.planning/phases/06-pure-foundations-ws-url-envelope-parsing/06-02-SUMMARY.md` when done.
</output>
