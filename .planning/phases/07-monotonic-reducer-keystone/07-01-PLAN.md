---
phase: 07-monotonic-reducer-keystone
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/game/gameStateReducer.js
  - src/game/gameStateReducer.test.js
autonomous: true
requirements: [STA-01, STA-02, RTT-01]

must_haves:
  truths:
    - "applyGameState(current, incoming) exists as the single exported reducer entry point"
    - "Applying a state with a newer play_counter returns the incoming reference (a new object)"
    - "Applying a state with an equal play_counter returns the EXACT SAME current reference (no re-render)"
    - "Applying a state with an older play_counter returns the exact same current reference (safe no-op)"
    - "Applying a state with missing/undefined/NaN play_counter is rejected as a no-op and logged via console.error, never thrown"
    - "When current has no play_counter field at all (bootstrap), the first valid incoming state always applies"
  artifacts:
    - src/game/gameStateReducer.js
    - src/game/gameStateReducer.test.js
  key_links:
    - "gameStateReducer.js exports applyGameState — the load-bearing contract consumed by Phase 7 Plan 2 (gameStore refactor), Phase 8 dispatchEvent, and Phase 9 useGameSocket resync"
---

<objective>
Create the single shared, `play_counter`-gated pure reducer `applyGameState(current, incoming)` in a new `src/game/gameStateReducer.js` module, mirroring the zero-mock pure-module style of `src/game/playOutcome.js` and `src/game/gameEvents.js`. This is the KEYSTONE artifact of the v1.1 milestone: every downstream `gameState` write (this phase's REST refactor, Phase 8's dispatch, Phase 9's resync) routes through this one function.

Purpose: Turn "idempotency" into true convergence — out-of-order or duplicate state applies must never regress or unnecessarily re-render, and stale/malformed input must never throw.
Output: `src/game/gameStateReducer.js` (single export `applyGameState`) + colocated `src/game/gameStateReducer.test.js` (zero-mock Vitest suite covering apply/no-op/bootstrap/reject cases per D-03 through D-06).
</objective>

<execution_context>
@/mnt/c/Users/taragin/OneDrive - Hewlett Packard Enterprise/Config/.config/opencode/gsd-core/workflows/execute-plan.md
@/mnt/c/Users/taragin/OneDrive - Hewlett Packard Enterprise/Config/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-monotonic-reducer-keystone/07-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD — implement applyGameState monotonic reducer</name>
  <files>src/game/gameStateReducer.js, src/game/gameStateReducer.test.js</files>
  <read_first>
    src/game/playOutcome.js (gold-standard pure-module structure: JSDoc on exports, no throwing, no Vue/store imports, defensive defaults)
    src/game/playOutcome.test.js (zero-mock Vitest suite style to mirror: describe/it blocks per exported function)
    src/game/gameEvents.js (D-06-style console.error-on-reject convention: context phrase + relevant identifier, no full payload dump)
    .planning/phases/07-monotonic-reducer-keystone/07-CONTEXT.md sections "Reducer location & API shape" (D-01..D-03) and "No-op / convergence semantics" (D-04..D-06)
  </read_first>
  <behavior>
    - Test: incoming.play_counter (5) > current.play_counter (3) → returns the incoming object by reference (result === incoming), not a copy-then-mutate check — assert `applyGameState(current, incoming) === incoming` or a shallow-copied object per discretion, but MUST NOT equal `current`.
    - Test: incoming.play_counter (3) === current.play_counter (3) → returns current by reference: `applyGameState(current, incoming) === current` (strict reference equality, not deep-equal).
    - Test: incoming.play_counter (2) < current.play_counter (3) → returns current by reference: `applyGameState(current, incoming) === current`.
    - Test: incoming.play_counter is undefined → returns current by reference; `console.error` called with a context-phrase string (spy via `vi.spyOn(console, 'error')`); function does not throw.
    - Test: incoming.play_counter is NaN (e.g. `Number('abc')` pre-computed, or literal `NaN`) → returns current by reference; console.error called; does not throw.
    - Test: incoming.play_counter is null → returns current by reference (treated as missing).
    - Test: incoming itself is null/undefined → returns current by reference; console.error called; does not throw (defensive against a bad caller).
    - Test: current has no play_counter field at all (bootstrap placeholder shape, e.g. `{ home_score: 0, away_score: 0 }` with no play_counter key) and incoming has play_counter: 1 → returns incoming by reference (bootstrap always applies per D-05).
    - Test: current is `{}` (empty object, no play_counter) and incoming.play_counter is missing too → returns current unchanged (reject wins even in bootstrap case when incoming is itself invalid).
  </behavior>
  <action>
    RED: Write `src/game/gameStateReducer.test.js` first, importing `applyGameState` from `@/game/gameStateReducer.js`, covering all cases in `<behavior>` above. Run tests — MUST fail (module does not exist yet).

    GREEN: Create `src/game/gameStateReducer.js` with a module header comment (mirroring `playOutcome.js`'s "Pure functions only — no store access" style) stating this is the single shared apply-path gate for both REST and WS. Export exactly one public function: `applyGameState(current, incoming)`.

    Internal decomposition (discretionary per D-01 "the agent's Discretion", but recommended): a private (non-exported) helper such as `isNewer(current, incoming)` that:
    - Reads `incoming?.play_counter`, validates it is a finite number via `Number.isFinite(...)` (rejects undefined, null, NaN, non-numeric strings) — reject (return false) if not finite.
    - Reads `current?.play_counter`. If `current` has no `play_counter` field (bootstrap, per D-05), treat current's counter as always-older (e.g. `-Infinity` or equivalent), so any valid incoming counter is newer.
    - Returns `incoming.play_counter > current.play_counter` (strictly greater — equal is NOT newer, per D-06).

    `applyGameState(current, incoming)` body: if `incoming` is null/undefined, log via `console.error` with a context phrase (e.g. "applyGameState: rejected incoming state — missing or invalid play_counter") and return `current` unchanged. Otherwise call the newness check; if newer, return `incoming` (new reference — per D-03/discretion, returning `incoming` directly by identity is acceptable, a shallow copy is also acceptable but not required). If not newer, log via `console.error` with a context phrase identifying the rejected play_counter value (not the full state payload — mirror `gameEvents.js`'s no-full-payload-dump convention) and return `current` by reference, unchanged (D-06's exact-reference guarantee — do NOT spread/copy `current` in the no-op path).

    JSDoc the exported `applyGameState` function per the repo's gold-standard style (`@param`, description of the gating behavior, note on the "same reference on no-op" contract for Vue reactivity).

    Run tests again — MUST pass. Commit RED then GREEN per TDD convention (`test(07-01): ...` then `feat(07-01): ...`).

    REFACTOR (if needed): clean up only if duplication exists; re-run tests, must still pass.
  </action>
  <verify>
    <automated>npx vitest run src/game/gameStateReducer.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/game/gameStateReducer.test.js` exits 0, all behavior cases pass
    - `src/game/gameStateReducer.js` contains the exact string `export function applyGameState(current, incoming)` (or equivalent named-export arrow form using the same identifier `applyGameState`)
    - Grep confirms no `import` of `vue`, `pinia`, `axios`, or any `src/stores/` path in `src/game/gameStateReducer.js` (pure module, zero framework coupling)
    - Grep confirms `src/game/gameStateReducer.js` contains no `throw` statement (never throws, per D-04)
    - Test file asserts strict reference equality (`toBe`, not `toEqual`) for both the equal-counter no-op case and the older-counter no-op case
    - `npx eslint src/game/gameStateReducer.js src/game/gameStateReducer.test.js` exits clean (no-semicolons, single-quote, 2-space per project ESLint/Prettier config)
  </acceptance_criteria>
  <done>applyGameState(current, incoming) exists, is pure, never throws, returns incoming (new reference) only on strictly-newer play_counter, returns current (exact same reference) on equal/older/missing/NaN/null incoming, and treats a current with no play_counter field as always-older (bootstrap). All cases covered by zero-mock Vitest tests, all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REST response body → applyGameState | Server-returned JSON `play_counter`/state fields are external, untrusted-shape input to the reducer (this phase); WS envelope data will cross the same boundary starting Phase 8 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Denial of Service | applyGameState | low | mitigate | Reducer never throws on malformed/missing/NaN `play_counter` (D-04) — a hostile or buggy server response cannot crash the client's reactive state layer; it is logged and safely rejected as a no-op |
| T-07-02 | Tampering | applyGameState | low | accept | The reducer trusts the server-authoritative `play_counter` ordering signal by design (backend-authoritative model, per PROJECT.md constraint) — a compromised/MITM'd backend could send a forged higher counter, but this is out of scope for a client-side reducer; TLS/transport security is the actual mitigation layer, already assumed by `VITE_API_BASE_URL` using https in production |
</threat_model>

<verification>
Run `npx vitest run src/game/gameStateReducer.test.js` — all tests pass, exit 0.
Run `npx eslint src/game/gameStateReducer.js src/game/gameStateReducer.test.js` — no lint errors.
Manually confirm via grep: no throw statements, no framework imports in the reducer module.
</verification>

<success_criteria>
`src/game/gameStateReducer.js` exports a single pure `applyGameState(current, incoming)` function satisfying D-01 through D-06: strictly-newer applies (new reference), equal/older/invalid rejects as no-op (exact same `current` reference), bootstrap (no `play_counter` on `current`) always yields to first valid incoming. Zero-mock test suite covers every case and passes.
</success_criteria>

<output>
Create `.planning/phases/07-monotonic-reducer-keystone/07-01-SUMMARY.md` when done
</output>
