---
phase: 07-monotonic-reducer-keystone
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/stores/gameStore.js
autonomous: true
requirements: [STA-01, RTT-01]

must_haves:
  truths:
    - "fetchGame() no longer writes gameState.value directly with a raw unconditional assignment — it routes through applyGameState"
    - "updateGameStateFromPlayResult(playResult) keeps its existing name and single-argument signature but its internals now call applyGameState(gameState.value, playResult.new_state)"
    - "fetchPlayResult()'s existing playResults array-push play_counter gate is untouched — it still independently gates before calling updateGameStateFromPlayResult"
    - "fetchAllPlayResults() still calls updateGameStateFromPlayResult unchanged at the call site"
    - "The existing zero-mock pure test suites for toWebSocketUrl (gameSocketUrl.test.js) and gameEvents.parseEvent (gameEvents.test.js) from Phase 6 still exist and pass, satisfying RTT-01's folded-in pure-module coverage requirement alongside the new reducer tests"
  artifacts:
    - src/stores/gameStore.js
  key_links:
    - "gameStore.js imports applyGameState from @/game/monotonicReducer.js — the single write path for gameState.value going forward, per the Phase 7 hard gate (nothing downstream may write gameState except through this reducer)"
---

<objective>
Refactor the existing REST call sites in `src/stores/gameStore.js` — `fetchGame()` and `updateGameStateFromPlayResult()` — to route every `gameState.value` write through the Phase 7 Plan 1 `applyGameState` reducer, per D-07 and D-08. This closes the hard gate: after this plan, no code path anywhere in the app writes `gameState.value` except through `applyGameState`. `fetchPlayResult()`'s independent `playResults` array-push gate (D-09) is explicitly NOT touched. This plan also confirms Phase 6's pure-module test suites still pass, satisfying RTT-01's combined "pure + reducer coverage" requirement.

Purpose: Satisfy the milestone's architectural keystone gate — REST optimistic behavior is preserved (additive refactor, not a rewrite), while establishing the single write path that Phase 8 (dispatch) and Phase 9 (WS resync) will build on.
Output: `src/stores/gameStore.js` refactored at exactly two call sites (`fetchGame`, `updateGameStateFromPlayResult`); confirmed-green existing pure test suites.
</objective>

<execution_context>
@/mnt/c/Users/taragin/OneDrive - Hewlett Packard Enterprise/Config/.config/opencode/gsd-core/workflows/execute-plan.md
@/mnt/c/Users/taragin/OneDrive - Hewlett Packard Enterprise/Config/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-monotonic-reducer-keystone/07-CONTEXT.md
@.planning/phases/07-monotonic-reducer-keystone/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Refactor fetchGame() and updateGameStateFromPlayResult() to route through applyGameState</name>
  <files>src/stores/gameStore.js</files>
  <read_first>
    src/stores/gameStore.js (live file — read fully before editing; line numbers in CONTEXT.md may have drifted)
    src/game/monotonicReducer.js (the reducer this plan wires in — exact export name and signature: applyGameState(current, incoming))
    .planning/phases/07-monotonic-reducer-keystone/07-CONTEXT.md sections "REST call-site migration scope" (D-07, D-08) and "playResults array vs gameState gating" (D-09)
    .planning/phases/07-monotonic-reducer-keystone/07-01-SUMMARY.md (confirms the reducer's actual exported behavior/reference contract as built)
  </read_first>
  <action>
    Add an import at the top of `src/stores/gameStore.js`: `import { applyGameState } from '../game/monotonicReducer.js'` (use a relative path consistent with the existing `import { SPFMetadata } from '../game/SPFMetadata.js'` line already in this file — do not introduce the `@` alias here since it would be the only aliased import in a file that otherwise uses relative paths for `src/game/` imports).

    In `fetchGame()`: replace the unconditional `gameState.value = response.data` assignment with `gameState.value = applyGameState(gameState.value, response.data)`. Per D-07, this is the bootstrap-apply call site — `gameState.value`'s initial placeholder object has no `play_counter` field, so the reducer's bootstrap rule (current has no play_counter → always older) ensures this first fetch always applies. Do not add any additional conditional logic around this call — the reducer owns the gating decision entirely.

    In `updateGameStateFromPlayResult(playResult)`: keep the function name and its single `playResult` parameter unchanged (D-08 — no call-site churn at `fetchPlayResult`/`fetchAllPlayResults`). Replace the internal body's unconditional `gameState.value = { ...playResult.new_state }` spread with a call to `applyGameState(gameState.value, playResult.new_state)`, assigning the result back to `gameState.value`. Preserve the existing guard `if (playResult && playResult.new_state)` before calling the reducer (extracting `.new_state` per D-02 — `applyGameState` takes only the resolved state object, never a play-result-shaped payload). Preserve the function's existing return value contract (`true`/`false` based on whether `playResult.new_state` was present) — this return value is unrelated to whether the reducer actually applied or rejected the state; do not change what the function returns.

    Do NOT touch `fetchPlayResult()`'s existing `play_counter` comparison logic that gates pushing onto the `playResults` array (D-09) — that gate stays exactly as-is, independent of the new reducer gate. Do NOT touch `fetchAllPlayResults()`'s call site to `updateGameStateFromPlayResult` — it already calls the function correctly; only the function's internals changed.
  </action>
  <verify>
    <automated>npx eslint src/stores/gameStore.js</automated>
  </verify>
  <acceptance_criteria>
    - `src/stores/gameStore.js` contains the exact string `import { applyGameState } from '../game/monotonicReducer.js'`
    - `src/stores/gameStore.js` no longer contains the literal `gameState.value = response.data` inside `fetchGame` (region-scoped: only this function's body, other `response.data` assignments elsewhere in the file for `gameMsg`/`lineups` etc. are unaffected and must remain)
    - `src/stores/gameStore.js` contains `gameState.value = applyGameState(gameState.value, response.data)` inside `fetchGame`
    - `src/stores/gameStore.js`'s `updateGameStateFromPlayResult` function body no longer contains `gameState.value = { ...playResult.new_state }` and instead calls `applyGameState(gameState.value, playResult.new_state)`
    - `updateGameStateFromPlayResult` still has the exact signature `function updateGameStateFromPlayResult(playResult)` (name and arity unchanged)
    - `fetchPlayResult`'s `newPlayCounter > mostRecentPlayCounter` array-push gate string is unchanged (still present verbatim)
    - `npx eslint src/stores/gameStore.js` exits 0
  </acceptance_criteria>
  <done>fetchGame() and updateGameStateFromPlayResult() both route their gameState.value writes through applyGameState; no other gameState.value write sites remain in the file; fetchPlayResult's independent array-push gate and fetchAllPlayResults' call site are unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Confirm folded-in RTT-01 pure-module coverage still passes</name>
  <files>src/game/gameSocketUrl.test.js, src/game/gameEvents.test.js, src/game/monotonicReducer.test.js</files>
  <read_first>
    src/game/gameSocketUrl.test.js (Phase 6 pure toWebSocketUrl test suite)
    src/game/gameEvents.test.js (Phase 6 pure gameEvents.parseEvent test suite)
    .planning/phases/07-monotonic-reducer-keystone/07-CONTEXT.md domain section (RTT-01's combined "pure + reducer coverage" requirement)
  </read_first>
  <action>
    This task performs no source edits — it is a verification-only task confirming RTT-01 is fully satisfied by the combination of Phase 6's existing pure-module tests and Phase 7 Plan 1's new reducer tests. Run the full trio of test files together via a single Vitest invocation and confirm all pass with no regressions introduced by Task 1's gameStore.js refactor (gameStore.js itself has no colocated test file in this codebase, so this check is limited to confirming the pure `src/game/` modules remain green — store-level test coverage for the refactored call sites is out of this phase's scope per CONTEXT.md and will be exercised in Phase 9's store-level tests).
  </action>
  <verify>
    <automated>npx vitest run src/game/gameSocketUrl.test.js src/game/gameEvents.test.js src/game/monotonicReducer.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/game/gameSocketUrl.test.js src/game/gameEvents.test.js src/game/monotonicReducer.test.js` exits 0 with all three suites passing
    - No test file modifications were made by this task (git diff shows zero changes to these three test files)
  </acceptance_criteria>
  <done>All three pure-module test suites (toWebSocketUrl, gameEvents.parseEvent, applyGameState) pass together in one run, confirming RTT-01's combined pure + reducer coverage requirement is met.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REST response body → gameStore.gameState | Server JSON now flows through applyGameState before touching store state — this plan closes the last unguarded write path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-03 | Tampering | fetchGame / updateGameStateFromPlayResult | low | mitigate | Both call sites now funnel through applyGameState's play_counter gate instead of unconditional assignment, closing the hard gate — no remaining code path can silently regress gameState with a stale/malformed server response |
</threat_model>

<verification>
Run `npx eslint src/stores/gameStore.js` — clean.
Run `npx vitest run src/game/gameSocketUrl.test.js src/game/gameEvents.test.js src/game/monotonicReducer.test.js` — all pass.
Grep `src/stores/gameStore.js` for `gameState.value =` — every remaining occurrence must be either the reducer call (`applyGameState(...)`) or the initial `ref({...})` declaration; no raw unconditional overwrite remains.
</verification>

<success_criteria>
Every `gameState.value` write in `src/stores/gameStore.js` routes through `applyGameState`. `fetchPlayResult`'s independent array-push gate and `fetchAllPlayResults`' call site are unchanged. Phase 6's pure test suites plus Phase 7's new reducer test suite all pass together, satisfying RTT-01.
</success_criteria>

<output>
Create `.planning/phases/07-monotonic-reducer-keystone/07-02-SUMMARY.md` when done
</output>
