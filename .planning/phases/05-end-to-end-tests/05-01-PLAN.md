---
phase: 05-end-to-end-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - playwright.config.js
  - e2e/fixtures/playFlowMocks.js
  - e2e/play-flow.spec.js
autonomous: true
requirements: [E2E-01, E2E-02, E2E-03, E2E-04]

must_haves:
  truths:
    - "`npx playwright test` boots the app via a `webServer` config (npm run dev) with no manual server start"
    - "The default `mocked` project runs the full play flow (landing -> start game -> pick play type -> assign lineup -> run play -> see result) with zero real network calls, using page.route() mocks"
    - "The mocked test chains two plays (D-03): after play 1 resolves, the UI reflects updated state before play 2 runs, proving state continuity"
    - "Mocked responses use full-shape new_state payloads (all gameState fields) matching what updateGameStateFromPlayResult expects, not partial stubs"
    - "Assertions are web-first (expect(locator).toBeVisible(), page.waitForResponse()) - no page.waitForTimeout() anywhere in the suite"
    - "npx playwright test --project=live runs the same spec file against a real backend at VITE_API_BASE_URL, with no route mocks installed"
  artifacts:
    - playwright.config.js
    - e2e/fixtures/playFlowMocks.js
    - e2e/play-flow.spec.js
  key_links:
    - "playwright.config.js webServer.command -> npm run dev, webServer.url -> Vite dev server origin"
    - "playwright.config.js projects[] -> mocked (default, use.baseURL = webServer origin) and live (use.baseURL = webServer origin; backend target comes from VITE_API_BASE_URL read by the app itself)"
    - "e2e/play-flow.spec.js reads testInfo.project.name === 'live' to decide whether to install page.route() mocks from the fixtures module"
    - "e2e/fixtures/playFlowMocks.js full-shape fixtures feed page.route() handlers for /players/away, /players/home, /game/state, /game/nexttype, /{offense|defense}/lineup, /offense/call or /defense/call, /game/play, /game/plays?result=true&count=1"
</must_haves>
---

<objective>
Install and configure Playwright, then deliver a single hermetic (route-mocked) end-to-end test of the core play flow that runs by default with no backend, plus an opt-in `--project=live` mode that exercises the same test against the real backend via `VITE_API_BASE_URL`.

Purpose: Prove the full play flow works from the browser's perspective (landing -> game -> two chained plays -> result), independent of backend availability, closing out the testing milestone (E2E-01..04).
Output: `playwright.config.js` (webServer + `mocked`/`live` projects), `e2e/fixtures/playFlowMocks.js` (full-shape response fixtures), `e2e/play-flow.spec.js` (single test file, internally branches mocked vs. live per D-07).
</objective>

<execution_context>
@/mnt/c/Users/taragin/OneDrive - Hewlett Packard Enterprise/Config/.config/opencode/gsd-core/workflows/execute-plan.md
@/mnt/c/Users/taragin/OneDrive - Hewlett Packard Enterprise/Config/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-end-to-end-tests/05-CONTEXT.md

# Prior-phase factory pattern (shape reference only - do NOT import test/factories/ into e2e/, per D-08)
@test/factories/gameState.js
@test/factories/lineup.js
@test/factories/players.js

# Exact endpoint sequence and response shapes to mock
@src/stores/gameStore.js
@src/stores/teamStore.js
@src/views/LandingPage.vue
@src/views/GameView.vue
@src/components/GameLayout.vue
@src/components/PlayTypeSelector.vue
@src/components/PlayLineup.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Playwright and configure webServer + mocked/live projects</name>
  <files>package.json, package-lock.json, playwright.config.js</files>
  <read_first>
    package.json (existing `test:e2e` script already reads `"playwright test"` per Phase 1 - do not change the script name), vite.config.js (dev server default port/behavior), .env / .env.development (VITE_API_BASE_URL=http://127.0.0.1:8080)
  </read_first>
  <action>
    Run `npm install -D @playwright/test` (do not add a separate `playwright` package - `@playwright/test` bundles the test runner). Do not run the `npx playwright install` browser-binary download as part of this task's automated verification (network/CI-dependent); note it in the SUMMARY as a one-time local/CI setup step.

    Create `playwright.config.js` at repo root using `defineConfig` from `@playwright/test`:
    - `testDir: './e2e'`
    - `fullyParallel: false` (single spec file, sequential two-play flow - avoid worker contention)
    - `webServer`: `{ command: 'npm run dev', url: 'http://localhost:5173', reuseExistingServer: !process.env.CI, timeout: 30000 }` (per D-04: use `npm run dev`, the Vite dev server, for fast startup with no build step before every E2E run) - Vite's default dev port is 5173; if `vite.config.js` sets a custom `server.port`, use that value consistently across `webServer.url` and every project's `use.baseURL` instead.
    - `projects`: array with exactly two entries, in this order (first entry is Playwright's default project when no `--project` flag is passed):
      - `{ name: 'mocked', use: { baseURL: 'http://localhost:5173' } }`
      - `{ name: 'live', use: { baseURL: 'http://localhost:5173' } }`
    - Add a code comment above `projects` explaining: the app itself always runs on the local Vite dev server (`baseURL` is identical for both projects); the `live` project's distinguishing behavior is "no route mocks installed" (per D-07, decided inside the single spec file via `testInfo.project.name`), and the *backend* target for `live` comes from `VITE_API_BASE_URL` which the dev server already reads from `.env`/`.env.development` - no Playwright-level env override is needed. This satisfies D-05/D-06's "project selection is the env-driven switch" resolution without introducing a custom `E2E_MODE` variable.
  </action>
  <verify>
    <automated>node -e "require('./playwright.config.js'); console.log('config-ok')"</automated>
  </verify>
  <acceptance_criteria>
    `@playwright/test` appears in `package.json` devDependencies; `playwright.config.js` exports a config with `testDir: './e2e'`, a `webServer` block whose `command` is `'npm run dev'`, and a `projects` array containing exactly `mocked` (first) and `live` entries; `node -e "require('./playwright.config.js')"` exits 0 with no throw.
  </acceptance_criteria>
  <done>playwright.config.js exists, requires cleanly, defines webServer + mocked/live projects; @playwright/test installed as devDependency.</done>
</task>

<task type="auto">
  <name>Task 2: Build full-shape mock response fixtures</name>
  <files>e2e/fixtures/playFlowMocks.js</files>
  <read_first>
    src/stores/gameStore.js (exact gameState shape: home_score, away_score, quarter, time_remaining, possession, yard_line, first_down_target, last_status, down; fetchPlayResult expects response.data as an array whose `[0].new_state.play_counter` must exceed the previously stored counter to be accepted), src/stores/teamStore.js (fetchPlayers issues two GETs: `${baseUrl}/players/away` then `${baseUrl}/players/home`, each response passed to `setTeam`), src/components/PlayTypeSelector.vue (`GET /game/nexttype` shape: `{ allowed_types: [...], next_type: '...' }`), test/factories/gameState.js and test/factories/players.js (field-name reference only - per D-08 do NOT import these; e2e fixtures are standalone HTTP response bodies, not Pinia-seed objects)
  </read_first>
  <action>
    Create `e2e/fixtures/playFlowMocks.js` exporting plain functions/objects, ES module syntax (project has no `"type": "module"` override needed since Playwright config files and e2e specs run under Playwright's own ESM/CJS handling - use `export const` / `export function`, matching the rest of the Vite-based repo's `import`/`export` style):
    - `awayPlayersResponse` and `homePlayersResponse` - response bodies for `GET {baseUrl}/players/away` and `GET {baseUrl}/players/home`, each a players-map object like `{ 'QB-1': { name: 'Away QB', id: 'QB-1' } }` matching the shape `teamStore.js`'s `setTeam` consumes.
    - `nextTypeResponse` - body for `GET {baseUrl}/game/nexttype`: `{ allowed_types: ['standard'], next_type: 'standard' }`.
    - `lineupAckResponse` - body for `POST {baseUrl}/offense/lineup` and `/defense/lineup` (string ack, e.g. `'Lineup set'`, matching `gameMsg.value = response.data`).
    - `playCallAckResponse` - body for `POST {baseUrl}/offense/call` / `/defense/call` (string ack).
    - `runPlayAckResponse` - body for `POST {baseUrl}/game/play` (string ack).
    - `buildGameStateResponse(overrides = {})` - full nine-field body for `GET {baseUrl}/game/state`: `home_score`, `away_score`, `quarter`, `time_remaining`, `possession`, `yard_line`, `first_down_target`, `last_status`, `down`, spread with `overrides`.
    - `buildPlayResultResponse(playCounter, overrides = {})` - returns an array `[{ result_type: 'Complete', result: 8, new_state: { ...buildGameStateResponse() fields, play_counter: playCounter, ...overrides } }]` for `GET {baseUrl}/game/plays?result=true&count=1` - parameterized by `playCounter` so the spec produces two distinct bodies (play 1 -> play_counter 1 with e.g. `down: 'Second'`; play 2 -> play_counter 2 with a further-advanced `down`/`yard_line`), satisfying `fetchPlayResult`'s counter-increase guard and giving the test a visible state change to assert between plays.
    Follow AGENTS.md style exactly: no semicolons, single quotes, 2-space indent, print width 100, no trailing commas, and a JSDoc block comment on every exported function (per CONVENTIONS.md's `playOutcome.js` gold-standard).
  </action>
  <verify>
    <automated>node --input-type=module -e "import('./e2e/fixtures/playFlowMocks.js').then(m => { const r = m.buildPlayResultResponse(2); if (!r[0].new_state.play_counter) throw new Error('shape'); console.log('fixtures-ok') })"</automated>
  </verify>
  <acceptance_criteria>
    `e2e/fixtures/playFlowMocks.js` exists and exports `awayPlayersResponse`, `homePlayersResponse`, `nextTypeResponse`, `lineupAckResponse`, `playCallAckResponse`, `runPlayAckResponse`, `buildGameStateResponse`, and `buildPlayResultResponse`; `buildGameStateResponse()` returns an object with all nine gameState keys; `buildPlayResultResponse(2)[0].new_state.play_counter === 2`; file has no semicolons and uses single quotes (spot-checkable via `grep -c ";" e2e/fixtures/playFlowMocks.js` returning `0`, excluding this instruction text itself).
  </acceptance_criteria>
  <done>e2e/fixtures/playFlowMocks.js exists, exports all listed fixtures with full-shape gameState/new_state payloads, follows AGENTS.md style.</done>
</task>

<task type="auto">
  <name>Task 3: Write the hermetic + live play-flow spec (two chained plays, mocked default)</name>
  <files>e2e/play-flow.spec.js</files>
  <read_first>
    e2e/fixtures/playFlowMocks.js (fixtures created in Task 2), playwright.config.js (mocked/live project names created in Task 1), src/views/LandingPage.vue (Start Game button disabled until teamsStore.fetchPlayers resolves; button text "Play"), src/views/GameView.vue (onMounted fires teamsStore.fetchPlayers() and gameStore.fetchGame() in parallel), src/components/PlayTypeSelector.vue (v-btn-toggle calling gameStore.setPlayType on click; onMounted calls gameStore.fetchPlayTypes -> GET /game/nexttype), src/components/PlayLineup.vue ("Submit Lineup" v-btn calling submitLineup -> setLineup -> POST /{offense|defense}/lineup; lineupSubmitted chip "Lineup set" appears after), src/components/GameLayout.vue ("Run Play" v-btn disabled until lineupSubmitted, calls gamesStore.runPlay() then gamesStore.fetchGameData(false) which chains GET /game/state, GET /game/nexttype, GET /game/plays?result=true&count=1), src/components/PlayResult.vue (renders playResult; empty when none)
  </read_first>
  <action>
    Create `e2e/play-flow.spec.js` importing `{ test, expect }` from `@playwright/test` and the fixtures from `./fixtures/playFlowMocks.js`. Structure:

    1. Define a local `installMocks(page)` helper inside the spec (per D-07, one file, internal branching) that registers `page.route()` handlers for each exact endpoint (per D-01: mock `teamStore.fetchPlayers()`'s `GET /players/*` so the landing page's "Start Game"/"Play" button becomes available; per D-02: mock the full `/game` play-flow round trip): `**/players/away` -> `awayPlayersResponse`, `**/players/home` -> `homePlayersResponse`, `**/game/state` -> `buildGameStateResponse()`, `**/game/nexttype` (GET) -> `nextTypeResponse`, `**/offense/lineup` and `**/defense/lineup` (POST) -> `lineupAckResponse`, `**/offense/call` and `**/defense/call` (POST) -> `playCallAckResponse`, `**/game/play` (POST) -> `runPlayAckResponse`, and `**/game/plays*` (GET, matching the `?result=true&count=1` query) -> a route handler that alternates between `buildPlayResultResponse(1)` on its first invocation and `buildPlayResultResponse(2)` on its second (track invocation count via a closure variable), fulfilling with `route.fulfill({ json: ... })` for JSON bodies and `route.fulfill({ body: ... })` for the plain-string ack bodies.
    2. In a `test.beforeEach(async ({ page }, testInfo) => { ... })`, call `await installMocks(page)` only `if (testInfo.project.name !== 'live')` - this is the D-07 internal branch; the `live` project runs with zero `page.route()` calls installed, hitting the real backend at whatever `VITE_API_BASE_URL` the dev server was started with.
    3. Write one `test('completes two chained plays end to end', async ({ page }) => { ... })`:
       - `await page.goto('/')`
       - Assert the "Play" button becomes enabled: `await expect(page.getByRole('button', { name: 'Play' })).toBeEnabled()` (web-first assertion - Playwright auto-retries, no manual wait needed since it becomes enabled once `isLoading`/`loadError` clear).
       - Click it and assert navigation: `await page.getByRole('button', { name: 'Play' }).click()`, then `await expect(page).toHaveURL(/\/game$/)`.
       - Wait for the play-type fetch to resolve via `await page.waitForResponse(resp => resp.url().includes('/game/nexttype') && resp.status() === 200)` (or rely on the visible chip), then select a play type via the `v-btn-toggle` (`await page.getByRole('button', { name: /standard/i }).click()` or whatever the fixture's `nextTypeResponse.next_type` renders as).
       - Submit the lineup: click "Submit Lineup", then `await expect(page.getByText('Lineup set')).toBeVisible()`.
       - Run play 1: click "Run Play", use `await page.waitForResponse(resp => resp.url().includes('/game/plays') && resp.status() === 200)` to synchronize instead of a hard wait, then assert the result/state visibly changed (e.g. `await expect(page.getByText(/Second/)).toBeVisible()` matching play 1's mocked `down` value, or assert `PlayResult` shows content).
       - Repeat lineup-submit + play-type-select + "Run Play" for play 2, again synchronizing via `page.waitForResponse()` on `/game/plays`, and assert the play-2-specific mocked value (e.g. a further-advanced `down`/`yard_line`) is now visible - this is the D-03 "prove state continuity across two plays" assertion.
    4. Do not use `page.waitForTimeout()` anywhere (E2E-03 web-first constraint) - every synchronization point must be `expect(locator).toBeVisible/toBeEnabled()`, `page.waitForResponse()`, or `page.waitForURL()`.
    Follow AGENTS.md style: no semicolons, single quotes, 2-space indent, no trailing commas.
  </action>
  <verify>
    <automated>node -e "require('./playwright.config.js')" &amp;&amp; grep -c "waitForTimeout" e2e/play-flow.spec.js | grep -qx 0 &amp;&amp; echo NO_HARD_WAITS</automated>
  </verify>
  <acceptance_criteria>
    `e2e/play-flow.spec.js` exists, imports `test`/`expect` from `@playwright/test` and fixtures from `./fixtures/playFlowMocks.js`; contains exactly one `test(...)` block that drives landing -> game -> two full lineup+play-call+run-play cycles; `beforeEach` branches on `testInfo.project.name !== 'live'` before installing mocks; `grep -c 'waitForTimeout' e2e/play-flow.spec.js` returns `0`; running `npx playwright test --project=mocked --list` (no browser download required for `--list`) enumerates the one test with no config/parse error.
  </acceptance_criteria>
  <done>e2e/play-flow.spec.js exists, covers the full two-play hermetic flow with web-first assertions, and internally skips mock installation for the live project.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser (Playwright) -> Vite dev server -> backend | The `mocked` project intercepts all requests at the browser/network boundary via `page.route()`; the `live` project lets requests reach a real backend at `VITE_API_BASE_URL` |
| Test runner -> local dev server process | `webServer.command` spawns `npm run dev` as a child process during test runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | e2e/fixtures/playFlowMocks.js mock payloads | low | accept | Fixtures are static local test data, never sent over a real network boundary in the `mocked` project; no user-controllable input flows into them |
| T-05-02 | Information Disclosure | `live` project hitting a real backend with `VITE_API_BASE_URL` | medium | accept | `live` mode is opt-in only (`--project=live`), never runs by default in CI/local `npm run test:e2e`, and reuses the existing `.env`/`.env.development` value already trusted by `npm run dev` - no new credential surface introduced |
| T-05-SC | Tampering | npm install of `@playwright/test` | high | mitigate | `@playwright/test` is Microsoft's official first-party package (millions of weekly downloads, verified publisher) - not `[ASSUMED]`/`[SUS]`; no blocking human-verify checkpoint required per the legitimacy gate's own criteria, but executor must confirm the installed version matches the official npm registry listing before committing `package-lock.json` |

## Note on Package Legitimacy Gate

No RESEARCH.md exists for this phase (research skipped per orchestrator flag). The only package-manager install task is `@playwright/test`, a well-known first-party Microsoft package with no legitimacy ambiguity. Per the fallback policy, it is treated as verified-by-inspection rather than `[ASSUMED]`: Task 1's `<action>` already names the exact package (`@playwright/test`, not a similarly-named typosquat), and the executor should confirm on `npmjs.com/package/@playwright/test` before running the install if any doubt exists.
</threat_model>

<verification>
1. `npm install -D @playwright/test` succeeds and appears in `package.json` devDependencies (Task 1).
2. `node -e "require('./playwright.config.js')"` exits 0 with a valid config object containing `testDir`, `webServer`, and `projects: [mocked, live]` (Task 1).
3. `node --input-type=module -e "import('./e2e/fixtures/playFlowMocks.js')..."` confirms all fixture exports resolve and `buildPlayResultResponse` produces the correct `play_counter` (Task 2).
4. `grep -c 'waitForTimeout' e2e/play-flow.spec.js` returns `0` (Task 3, E2E-03).
5. `npx playwright install chromium` (one-time, network-dependent, executor runs manually if browsers are missing) then `npx playwright test --project=mocked` passes locally with the dev server auto-started by `webServer` and zero real network calls.
6. `npx playwright test --project=live` (manual, requires a running real backend at `VITE_API_BASE_URL`) exercises the same spec against the live server - not asserted by CI/automated verify in this plan since it depends on external backend availability, but the code path must exist and skip mock installation.
</verification>

<success_criteria>
- Playwright is installed and `playwright.config.js` configures a `webServer` booting the Vite app (E2E-01).
- The core play flow (start game -> pick type -> assign lineup -> run play -> see result), chained across two plays, passes hermetically under the `mocked` project with zero backend calls (E2E-02, D-03).
- All mocked responses are full-shape (`new_state` carries all nine gameState fields) and every synchronization point in the spec is a web-first assertion or `page.waitForResponse()` - no `page.waitForTimeout()` (E2E-03).
- `--project=live` toggles the same spec to skip mock installation and hit the real backend via the dev server's existing `VITE_API_BASE_URL` (E2E-04).
</success_criteria>

## Artifacts this phase produces

- `playwright.config.js` - Playwright config: `testDir: './e2e'`, `webServer` (`npm run dev`), `projects: [mocked, live]` (Task 1)
- `package.json` / `package-lock.json` - adds `@playwright/test` devDependency (Task 1); existing `test:e2e` script unchanged
- `e2e/` - new top-level directory for all E2E test assets
- `e2e/fixtures/playFlowMocks.js` - exports `awayPlayersResponse`, `homePlayersResponse`, `nextTypeResponse`, `lineupAckResponse`, `playCallAckResponse`, `runPlayAckResponse`, `buildGameStateResponse(overrides)`, `buildPlayResultResponse(playCounter, overrides)` (Task 2)
- `e2e/play-flow.spec.js` - single spec file; local `installMocks(page)` helper + `beforeEach` branching on `testInfo.project.name !== 'live'`; one `test(...)` driving landing -> game -> two chained plays (Task 3)


<output>
Create `.planning/phases/05-end-to-end-tests/05-01-SUMMARY.md` when done.
</output>