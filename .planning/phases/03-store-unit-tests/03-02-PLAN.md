---
phase: 03-store-unit-tests
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/stores/gameStore.test.js
autonomous: true
requirements: [STO-01, STO-02]
must_haves:
  truths:
    - "gameStore play-flow actions run against a fresh Pinia per test with axios mocked"
    - "Every error-bearing action tests BOTH the err.response and network/err.message branches"
    - "runPlay's network branch is asserted to reject (re-throw)"
    - "Every action's finally block is asserted to reset its is*ing loading flag"
  artifacts:
    - src/stores/gameStore.test.js
  key_links:
    - "vi.mock('axios') module-level + fresh Pinia in beforeEach guarantees no state/mock leakage"
    - "Assertions target error.value, gameMsg.value, and is*ing flags — the real regression surface"
---

<objective>
Vertical slice: a runnable, passing gameStore test suite covering play-flow success paths, EXHAUSTIVE error branches (D-02), and finally-block loading-flag resets (D-03), consuming the wave-1 factories.

Purpose: Lock down gameStore's play-flow and error UX so a stuck spinner or missed error message becomes a failing test.
Output: `src/stores/gameStore.test.js`.
</objective>

<execution_context>
@/mnt/c/Users/taragin/OneDrive - Hewlett Packard Enterprise/Config/.config/opencode/gsd-core/workflows/execute-plan.md
@/mnt/c/Users/taragin/OneDrive - Hewlett Packard Enterprise/Config/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-store-unit-tests/03-CONTEXT.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@src/stores/gameStore.js
</context>

## Artifacts this phase produces (this plan)

- File `src/stores/gameStore.test.js` — co-located gameStore suite (per Phase 1 convention)

<tasks>

<task type="auto">
  <name>Task 1: gameStore success-path play-flow suite (STO-01)</name>
  <files>src/stores/gameStore.test.js</files>
  <read_first>
    - src/stores/gameStore.js (all actions: setLineup, getLineup, fetchPlayTypes, setDefensivePlay, setOffensivePlay, setKickoffPlay, runPlay, setPlayType, fetchGame)
    - .planning/codebase/TESTING.md (Pinia store test example: vi.mock('axios'), setActivePinia(createPinia()) in beforeEach)
    - test/factories/gameState.js, test/factories/lineup.js (built in plan 03-01)
    - .planning/phases/03-store-unit-tests/03-CONTEXT.md (D-01, D-03, D-05, D-06)
  </read_first>
  <action>
    Create `src/stores/gameStore.test.js`. Declare `vi.mock('axios')` at module level (D-01), import axios, useGameStore from '@/stores/gameStore', setActivePinia/createPinia from pinia, and buildGameState/buildLineup from the '@/../test/factories' (use the working relative or configured path). Add `beforeEach(() => { setActivePinia(createPinia()); vi.clearAllMocks() })` to guarantee fresh Pinia + no mock leakage (D-05). Do NOT test getHardCodedValue (D-06).
    Add flat describe blocks per action for the SUCCESS path only in this task, wiring behavior with axios.get.mockResolvedValueOnce / axios.post.mockResolvedValueOnce: setLineup (asserts gameMsg.value set from response.data, lineups populated, isSubmittingLineup reset to false), getLineup (lineups[team] populated), fetchPlayTypes (playTypes and nextPlayType set from allowed_types/next_type), setDefensivePlay and setOffensivePlay and setKickoffPlay (gameMsg set, isSubmittingPlay reset to false), setPlayType (gameMsg set), runPlay (gameMsg set, lineupSubmitted false, isRunningPlay reset to false). Seed payloads with buildGameState()/buildLineup() where a game-state or lineup shape is returned. For each success case also assert the matching is*ing flag ends false (D-03).
    Prettier conventions: no semicolons, single quotes, 2-space indent, print width 100, no trailing commas. No fenced code in comments.
  </action>
  <verify>
    <automated>npx vitest run src/stores/gameStore.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/stores/gameStore.test.js` exits 0
    - File declares `vi.mock('axios')` at module level and `setActivePinia(createPinia())` in beforeEach
    - Contains describe/it success cases for setLineup, getLineup, fetchPlayTypes, setDefensivePlay, setOffensivePlay, setKickoffPlay, setPlayType, runPlay
    - No test references getHardCodedValue
    - Each success case asserts its is*ing flag is false after the await
  </acceptance_criteria>
  <done>Success-path suite for all listed play-flow actions passes green with fresh Pinia + mocked axios.</done>
</task>

<task type="auto">
  <name>Task 2: gameStore exhaustive error-branch + finally-reset suite (STO-02)</name>
  <files>src/stores/gameStore.test.js</files>
  <read_first>
    - src/stores/gameStore.js (error branches: note setLineup/getLineup/setPlayType/fetchPlayTypes use explicit if(err.response){...}else{...}; setDefensivePlay/setOffensivePlay/setKickoffPlay use `err.response ? err.response.data : err.message`; runPlay's else branch re-throws)
    - src/stores/gameStore.test.js (the success suite from Task 1 — extend it)
    - .planning/phases/03-store-unit-tests/03-CONTEXT.md (D-02 exhaustive branch list, D-03 finally reset)
  </read_first>
  <action>
    Extend `src/stores/gameStore.test.js` with error-branch describe/it blocks for every action having two branches (D-02): setLineup, getLineup, setDefensivePlay, setOffensivePlay, setKickoffPlay, setPlayType, fetchPlayTypes, runPlay. For EACH action test BOTH branches:
    - err.response branch: wire axios.post/get.mockRejectedValueOnce({ response: { data: 'bad thing' } }); assert error.value is set to the action's failure string containing the server data, assert gameMsg.value is set to the server data where the action sets it (setLineup, getLineup, setDefensivePlay/Offensive/Kickoff, setPlayType, fetchPlayTypes do), and assert error.value is non-null. Match each action's exact message-extraction variant (explicit if/else vs ternary) — read the source to get the wording.
    - network/err.message branch: wire mockRejectedValueOnce({ message: 'network down' }) (no response key); assert error.value falls back to the action's no-response string. For the ternary actions (setDefensivePlay/Offensive/Kickoff) assert gameMsg.value gets err.message.
    - runPlay network branch additionally re-throws: assert `await expect(store.runPlay()).rejects.toBeTruthy()` (the else branch calls throw err) while error.value is still set.
    For EVERY error case (both branches) also assert the matching is*ing flag (isSubmittingLineup / isSubmittingPlay / isRunningPlay) is reset to false by the finally block (D-03), including the runPlay reject case.
  </action>
  <verify>
    <automated>npx vitest run src/stores/gameStore.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/stores/gameStore.test.js` exits 0
    - Each of setLineup, getLineup, setDefensivePlay, setOffensivePlay, setKickoffPlay, setPlayType, fetchPlayTypes, runPlay has BOTH an err.response test and a network/err.message test
    - The runPlay network test asserts the returned promise rejects (contains a `.rejects` assertion inside a describe for runPlay)
    - Every error test asserts the relevant is*ing flag is false after the action settles
  </acceptance_criteria>
  <done>Exhaustive error-branch coverage green; every branch asserts error.value, message text, and the finally-reset loading flag.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/stores/gameStore.test.js` exits 0
- `npx vitest run` (full suite) still passes — no cross-file leakage
- `npm run lint` reports no new errors in src/stores/gameStore.test.js
</verification>

<success_criteria>
gameStore play-flow actions tested with fresh Pinia + mocked axios (criterion 1, STO-01); failure paths set error and reset loading flags in finally, verified against the app's error-extraction shape (criterion 2, STO-02).
</success_criteria>

<output>
Create `.planning/phases/03-store-unit-tests/03-02-SUMMARY.md` when done
</output>
