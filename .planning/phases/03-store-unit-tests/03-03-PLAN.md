---
phase: 03-store-unit-tests
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/stores/teamStore.test.js
autonomous: true
requirements: [STO-03]
must_haves:
  truths:
    - "teamStore fetchPlayers is tested with mocked axios against a fresh Pinia per test"
    - "fetchPlayers error branch is tested and isLoading is reset in finally"
    - "selectPlayer and removePlayer mutate playerPositions and player availability correctly"
    - "toggleManagedTeam swaps managed/other team and re-points the active team ref"
  artifacts:
    - src/stores/teamStore.test.js
  key_links:
    - "buildRoster()/buildPlayer() factories seed the two-team fetchPlayers response and TeamData construction"
    - "Fresh Pinia + vi.mock('axios') guarantees isolation (runs together / randomized)"
---

<objective>
Vertical slice: a runnable, passing teamStore test suite covering roster fetch (success + error + finally reset), player selection/removal, and the managed-team toggle, consuming the wave-1 player factory.

Purpose: Lock down teamStore's roster/lineup logic so player-assignment regressions surface as test failures.
Output: `src/stores/teamStore.test.js`.
</objective>

<execution_context>
@/mnt/c/Users/taragin/OneDrive - Hewlett Packard Enterprise/Config/.config/opencode/gsd-core/workflows/execute-plan.md
@/mnt/c/Users/taragin/OneDrive - Hewlett Packard Enterprise/Config/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-store-unit-tests/03-CONTEXT.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@src/stores/teamStore.js
@src/game/TeamData.js
</context>

## Artifacts this phase produces (this plan)

- File `src/stores/teamStore.test.js` — co-located teamStore suite (per Phase 1 convention)

<tasks>

<task type="auto">
  <name>Task 1: teamStore fetchPlayers success + error + finally suite (STO-03)</name>
  <files>src/stores/teamStore.test.js</files>
  <read_first>
    - src/stores/teamStore.js (fetchPlayers: two sequential axios.get calls to /players/away then /players/home, setTeam wraps each in new TeamData, version++, team re-pointed to managedTeam; catch logs only; finally resets isLoading)
    - src/game/TeamData.js (constructor consumes { players, team }; getPlayerByID, availablePlayerIDs)
    - .planning/codebase/TESTING.md (fresh-Pinia + vi.mock('axios') pattern)
    - test/factories/players.js (buildRoster/buildPlayer from plan 03-01)
    - .planning/phases/03-store-unit-tests/03-CONTEXT.md (D-01, D-03, D-05)
  </read_first>
  <action>
    Create `src/stores/teamStore.test.js` with module-level `vi.mock('axios')` (D-01), importing axios, useTeamsStore from '@/stores/teamStore', setActivePinia/createPinia from pinia, and buildRoster/buildPlayer from the test factories. Add `beforeEach(() => { setActivePinia(createPinia()); vi.clearAllMocks() })` (D-05).
    Success case: wire two axios.get.mockResolvedValueOnce calls returning buildRoster()-shaped payloads for away then home (order matters — away is fetched first). Call `await store.fetchPlayers()`. Assert isLoading ends false (D-03), version incremented, and the active team ref exposes the managed team's players (availablePlayerIDs / allPlayers reflect the seeded roster).
    Error case: wire axios.get.mockRejectedValueOnce (both an err.response variant and an err.request/network variant across two it blocks — fetchPlayers logs different console.error branches but sets no error ref). For each assert isLoading is reset to false by finally (D-03), and that fetchPlayers does not throw (it swallows). Use vi.spyOn(console, 'error') to assert the catch branch was hit without failing on console noise.
    Prettier conventions: no semicolons, single quotes, 2-space indent, print width 100, no trailing commas.
  </action>
  <verify>
    <automated>npx vitest run src/stores/teamStore.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/stores/teamStore.test.js` exits 0
    - File declares `vi.mock('axios')` and `setActivePinia(createPinia())` in beforeEach
    - fetchPlayers has a success test asserting isLoading false + version incremented, and at least two error tests (err.response and network) each asserting isLoading reset to false and no throw
  </acceptance_criteria>
  <done>fetchPlayers success and both error branches pass green with the loading flag reset asserted.</done>
</task>

<task type="auto">
  <name>Task 2: teamStore player-selection + managed-team-toggle suite (STO-03)</name>
  <files>src/stores/teamStore.test.js</files>
  <read_first>
    - src/stores/teamStore.js (selectPlayer assigns player + records playerPositions[spot]; removePlayer resets + deletes; toggleManagedTeam swaps managedTeam/otherTeam and re-points team.value to teams[managedTeam])
    - src/game/TeamData.js (assignPlayer/resetPlayer/availablePlayerIDs semantics)
    - src/stores/teamStore.test.js (the suite from Task 1 — extend it)
    - test/factories/players.js (buildRoster/buildPlayer)
  </read_first>
  <action>
    Extend `src/stores/teamStore.test.js` with describe blocks for selectPlayer/removePlayer and toggleManagedTeam.
    selectPlayer: after seeding a team (either via a fetchPlayers success wiring or by calling setTeam with buildRoster() output for the managed side), call selectPlayer(id, spot); assert playerPositions[spot] equals the player record for that id and that the id is removed from availablePlayerIDs (assigned). Then call selectPlayer of a different id into the SAME spot and assert the previously-assigned player is reset back to available (covers the curr-reset branch). removePlayer(spot): assert playerPositions[spot] is deleted and the player id returns to available.
    toggleManagedTeam: seed both teams via setTeam(buildRoster(), 'Home') and setTeam(buildRoster(), 'Away'); capture getManagedTeam(); call toggleManagedTeam(); assert getManagedTeam() flipped and homeTeam/awayTeam computed still resolve, and that team.value now points at the newly-managed side's TeamData.
    Keep fresh-Pinia isolation (beforeEach already resets). Prettier conventions as above.
  </action>
  <verify>
    <automated>npx vitest run src/stores/teamStore.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/stores/teamStore.test.js` exits 0
    - Contains a describe for selectPlayer asserting playerPositions[spot] is set and availability changes, including the reassignment-into-same-spot reset branch
    - Contains a removePlayer test asserting deletion + return-to-available
    - Contains a toggleManagedTeam test asserting managedTeam flips and team ref re-points
  </acceptance_criteria>
  <done>Player selection/removal and managed-team toggle logic pass green with a fresh Pinia per test.</done>
</task>

</tasks>

<verification>
- `npx vitest run src/stores/teamStore.test.js` exits 0
- `npx vitest run` (full suite) passes — store suites run together with no Pinia state leakage (criterion 5)
- `npm run lint` reports no new errors in src/stores/teamStore.test.js
</verification>

<success_criteria>
teamStore player/lineup logic (fetchPlayers, selectPlayer, managed-team toggle) tested with mocked axios (criterion 3, STO-03); store suite passes run-together with no Pinia state leakage (criterion 5).
</success_criteria>

<output>
Create `.planning/phases/03-store-unit-tests/03-03-SUMMARY.md` when done
</output>
