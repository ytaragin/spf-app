---
phase: 03-store-unit-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/factories/gameState.js
  - test/factories/lineup.js
  - test/factories/players.js
  - test/factories/factories.test.js
autonomous: true
requirements: [STO-04]
must_haves:
  truths:
    - "Tests can build a valid gameState payload with buildGameState() and override any field"
    - "Tests can build offense/defense lineup payloads with buildLineup()"
    - "Tests can build player records and rosters with buildPlayer()/buildRoster()"
  artifacts:
    - test/factories/gameState.js
    - test/factories/lineup.js
    - test/factories/players.js
  key_links:
    - "Factory field names match real backend snake_case shape (reusable by Phases 4-5)"
---

<objective>
Create the 3 reusable test factories in `test/factories/` (per D-04) that both store test suites (plans 02, 03) and future phases (4-5) consume. This is the wave-1 vertical slice: a runnable, self-verifying factories increment.

Purpose: Centralize backend-shaped seed data so store/component/E2E tests customize only what they care about via an overrides object.
Output: `test/factories/gameState.js`, `test/factories/lineup.js`, `test/factories/players.js`, plus a smoke test proving they run.
</objective>

<execution_context>
@/mnt/c/Users/taragin/OneDrive - Hewlett Packard Enterprise/Config/.config/opencode/gsd-core/workflows/execute-plan.md
@/mnt/c/Users/taragin/OneDrive - Hewlett Packard Enterprise/Config/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-store-unit-tests/03-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
</context>

## Phase Goal

**As a** developer writing store/component/E2E tests, **I want to** build backend-shaped game-state, lineup, and player fixtures from one place with per-test overrides, **so that** every test layer shares one reusable, realistic seed shape instead of ad-hoc inline literals.

## Artifacts this phase produces (this plan)

- File `test/factories/gameState.js` — exports `buildGameState(overrides = {})`
- File `test/factories/lineup.js` — exports `buildLineup(overrides = {})`
- File `test/factories/players.js` — exports `buildPlayer(overrides = {})` and `buildRoster(overrides = {})`
- File `test/factories/factories.test.js` — smoke test for the three factories

<tasks>

<task type="auto">
  <name>Task 1: Create the three factory modules</name>
  <files>test/factories/gameState.js, test/factories/lineup.js, test/factories/players.js</files>
  <read_first>
    - src/stores/gameStore.js (default gameState ref, lines 13-23; lineups shape via setLineup/getLineup)
    - src/stores/teamStore.js (default team ref seed players, line 16; TeamData usage)
    - src/game/TeamData.js (player records accessed as players[id].position and .id — records need name, id, position)
    - .planning/phases/03-store-unit-tests/03-CONTEXT.md (D-04 factory contract)
    - .planning/codebase/CONVENTIONS.md (snake_case server field names must NOT be renamed)
  </read_first>
  <action>
    Create `test/factories/gameState.js` exporting `buildGameState(overrides = {})` that returns an object with the server snake_case fields matching gameStore's default gameState ref: home_score (0), away_score (0), quarter (1), time_remaining ('15:00'), possession ('Home'), yard_line (25), first_down_target (35), last_status ('Start'), down ('First'). Spread `overrides` last so callers override individual fields (e.g. buildGameState({ down: 'Second' })).
    Create `test/factories/lineup.js` exporting `buildLineup(overrides = {})` returning an offense/defense lineup object shaped like what setLineup posts and getLineup receives — a plain map of position keys to player-id values (e.g. QB, RB, WR arrays/ids). Spread `overrides` last.
    Create `test/factories/players.js` exporting `buildPlayer(overrides = {})` returning a single record with name, id, and position fields (position is required because TeamData.getPlayersForPositions reads players[id].position); and `buildRoster(overrides = {})` returning a players map keyed by id (default two players mirroring teamStore's seed: QB-1 and RB-1 with position values), where `overrides` REPLACES the default player map entirely (per specifics note: factories override seed shape, not append). buildRoster should also allow overriding the wrapping shape TeamData expects: { team, availablePlayers, players }.
    Follow Prettier conventions exactly: no semicolons, single quotes, 2-space indent, print width 100, no trailing commas. Use plain ESM `export function` declarations. No fenced comments echoing prohibited literals.
  </action>
  <verify>
    <automated>node -e "import('./test/factories/gameState.js').then(m=>{if(m.buildGameState().down!=='First')process.exit(1)}).catch(()=>process.exit(1))"</automated>
  </verify>
  <acceptance_criteria>
    - test/factories/gameState.js exports buildGameState and buildGameState().home_score === 0 and buildGameState({ down: 'Second' }).down === 'Second'
    - test/factories/lineup.js exports buildLineup returning an object
    - test/factories/players.js exports both buildPlayer and buildRoster; buildPlayer() has name, id, position; buildRoster() returns a { players } shape TeamData can consume
  </acceptance_criteria>
  <done>All three factory files exist with the exact exported signatures from D-04 and pass Prettier formatting.</done>
</task>

<task type="auto">
  <name>Task 2: Add factories smoke test</name>
  <files>test/factories/factories.test.js</files>
  <read_first>
    - test/factories/gameState.js, test/factories/lineup.js, test/factories/players.js (the files created in Task 1)
    - src/game/TeamData.js (to assert buildRoster output constructs a TeamData without throwing)
    - .planning/codebase/TESTING.md (test style)
  </read_first>
  <action>
    Create `test/factories/factories.test.js` importing the three factories and TeamData from '@/game/TeamData.js'. Add a describe block per factory with it blocks asserting: buildGameState() returns the 9 snake_case fields with correct defaults and that an override changes only the targeted field; buildLineup() returns an object and an override merges; buildPlayer() returns name/id/position and buildRoster() output can be passed to `new TeamData(...)` without throwing and exposes the seeded player ids. Use vitest globals (describe/it/expect) per the existing config. No axios, no Pinia needed here.
  </action>
  <verify>
    <automated>npx vitest run test/factories/factories.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/factories/factories.test.js` exits 0
    - The file contains describe blocks for buildGameState, buildLineup, and buildRoster
    - At least one test constructs `new TeamData(buildRoster())` and asserts it does not throw
  </acceptance_criteria>
  <done>Smoke test passes green, proving all three factories produce consumable shapes.</done>
</task>

</tasks>

<verification>
- `npx vitest run test/factories/factories.test.js` exits 0
- All three factory files exist and export the D-04 signatures
- `npm run lint` reports no new errors in test/factories/
</verification>

<success_criteria>
Shared game-state/player factories exist in `test/factories/` with overrides support and backend snake_case field names (ROADMAP success criterion 4, STO-04).
</success_criteria>

<output>
Create `.planning/phases/03-store-unit-tests/03-01-SUMMARY.md` when done
</output>
