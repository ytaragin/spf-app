---
phase: 08-event-dispatch-glue
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - src/game/gameEventDispatch.js
  - src/game/gameEventDispatch.test.js
  - src/stores/gameStore.js
  - src/stores/gameStore.test.js
autonomous: true
requirements: [EVT-02, EVT-03]

must_haves:
  truths:
    - "`dispatchEvent(store, { event: 'NextPlayTypeSet', data })` routes to `store.applyNextPlayType(...)`, accepting a bare string and also tolerating the `{ next_type }` object shape returned by `/game/nexttype` (EVT-02, D-10)."
    - "`dispatchEvent` covers exactly the 5 known variants — the same set as `KNOWN_VARIANTS` in `src/game/gameEvents.js`, no more and no less (ROADMAP success criterion #1)."
    - "An unknown event tag, a null/undefined/number/string envelope, and a non-string `event` tag each return `false`, log via `console.error`, call zero store actions, and never throw (D-04; edge:empty)."
    - "Dispatching the same `NextPlayTypeSet` twice yields the identical stored `nextPlayType` — a last-writer-wins assignment is naturally idempotent (edge:idempotency)."
    - "Ordering across events is enforced only by the Phase 7 `play_counter` gate on state-bearing variants; `NextPlayTypeSet` and lineup variants are deliberately last-writer-wins with no ordering guarantee, matching the backend-authoritative model (edge:ordering)."
    - "`dispatchEvent` is fully synchronous — no `async`, no `await`, no I/O — so a dispatch cannot be interleaved with another on the single-threaded browser event loop; each call is atomic with respect to the event loop (edge:concurrency)."
    - "Routing `fetchPlayResult` through `applyPlayResult` does not regress the lineup/play-call flow: `fetchGameData` is invoked only on mount and immediately after `runPlay()` (which already resets `lineupSubmitted`), and the reset is gated on the reducer actually advancing (D-13, D-14)."
    - statement: "A REST `fetchGameData` that applies a genuinely new play collapses `lineupSubmitted`, which is the intended flow transition and matches `runPlay()`'s pre-existing behavior."
      verification: backstop
  artifacts:
    - src/game/gameEventDispatch.test.js
    - src/stores/gameStore.js
  key_links:
    - "`fetchPlayTypes` and `setPlayType` (REST) and `NextPlayTypeSet` (WS) all call `applyNextPlayType` — one write site for `nextPlayType` (D-06)."
    - "`dispatchEvent`'s branch set ↔ `KNOWN_VARIANTS` in `src/game/gameEvents.js` — the two must stay in lockstep."
    - "`runPlay` → `fetchGameData(false)` → `fetchPlayResult` → `applyPlayResult` → `lineupSubmitted = false` — the D-13 redundancy path that must remain benign."
  prohibitions:
    - "`applyNextPlayType` must NOT silently POST — it is local-only; `setPlayType` remains the POSTing sibling and the two must stay distinguishable by name (D-05, D-06)."
    - "The `fetchPlayTypes` error path must NOT lose its reset to an empty `playTypes` array and a null `nextPlayType` during the refactor."
    - "Event payload contents must NOT appear in any log line emitted by the new branches (D-08, Phase 6 D-06)."
---

## Phase Goal

**As a** player in a live game, **I want to** see the next play type the server has selected as soon
as it is announced, **so that** the whole 5-variant event stream keeps my board in sync.

> ⚠ Planner note: see the flagged ROADMAP goal-format note in `08-01-PLAN.md`.

## Artifacts this phase produces

Created by **this plan**:

| Symbol | Kind | Location |
|--------|------|----------|
| `applyNextPlayType(type)` | new store action (exported) | `src/stores/gameStore.js` |
| `dispatchEvent` `NextPlayTypeSet` branch | new branch in existing function | `src/game/gameEventDispatch.js` |

Created by sibling plans in phase 08: `gameEventDispatch.js`, `dispatchEvent`, `makeFakeStore()`,
`applyPlayResult(playAndState)` (plan 08-01); `applyIncomingGameState(state)`,
`applyLineup(side, lineup)` (plan 08-02).

<flagged_assumptions>
**Unresolved edge-probe row — surfaced deliberately, not dropped.**

The deterministic edge-probe report returned one `unclassified` row against **EVT-03** that this
plan cannot resolve to a covered or backstopped criterion:

> EVT-03 / unclassified — "review manually"

Reason it stays unresolved: EVT-03's correctness depends on the **backend WebSocket payload
contract**, which is UNVERIFIED. D-10 and D-11 lock the per-variant `data` shapes as *assumptions*
derived from the existing REST payload shapes, and explicitly defer verification to Phase 11's
`routeWebSocket` E2E (RTT-03). No test written in Phase 8 can confirm the real wire shape.

Accepted mitigation (D-08): a wrong assumption degrades to "events ignored plus a loud
`console.error`", never to corrupt state. This is a recorded, deliberate risk — carry it forward
into the Phase 8 SUMMARY and into Phase 11 planning.
</flagged_assumptions>

<objective>
Close the phase: wire the fifth and final variant (`NextPlayTypeSet`) end to end, prove
`dispatchEvent`'s branch coverage matches `KNOWN_VARIANTS` exactly, and discharge the recorded D-13
redundancy risk by confirming the REST play path's newly-inherited `lineupSubmitted` reset does not
regress the lineup/play-call flow.

Purpose: this is the plan that makes ROADMAP success criteria #1 and #4 fully true and converts the
`<specifics>` "redundancy to verify" note into a verified fact or a documented fallback.

Output: `applyNextPlayType` on `gameStore` with all three call sites refactored, the final dispatch
branch, a variant-completeness test, and an explicit D-13 flow-regression test.
</objective>

<execution_context>
@/mnt/c/Users/taragin/OneDrive - Hewlett Packard Enterprise/Config/.config/opencode/gsd-core/workflows/execute-plan.md
@/mnt/c/Users/taragin/OneDrive - Hewlett Packard Enterprise/Config/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-event-dispatch-glue/08-CONTEXT.md
@.planning/phases/08-event-dispatch-glue/08-PATTERNS.md
@.planning/phases/08-event-dispatch-glue/08-01-SUMMARY.md
@.planning/phases/08-event-dispatch-glue/08-02-SUMMARY.md
@src/game/gameEvents.js
@src/stores/gameStore.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add `applyNextPlayType` and refactor `fetchPlayTypes` and `setPlayType`</name>
  <files>src/stores/gameStore.js, src/stores/gameStore.test.js</files>
  <read_first>
    - `src/stores/gameStore.js` — `fetchPlayTypes` (success path AND error-reset path, which writes BOTH `playTypes` and `nextPlayType`), `setPlayType` (which currently writes NEITHER), `getNextPlayType`, `getPlayTypes`, and the `// Local-apply actions (do not POST)` return group.
    - `src/stores/gameStore.test.js` — the `fetchPlayTypes` test and the `setPlayType` test (the latter asserts only `gameMsg`, so an additive optimistic write does not break it).
    - `.planning/phases/08-event-dispatch-glue/08-PATTERNS.md` — sections "`fetchPlayTypes` — refactor target for `applyNextPlayType`" (Finding 2) and "`setPlayType` — third `applyNextPlayType` call site" (Finding 1, the additive-behavior warning).
    - `.planning/phases/08-event-dispatch-glue/08-CONTEXT.md` — D-05, D-06, D-07, D-10.
  </read_first>
  <behavior>
    - `store.applyNextPlayType('Run')` makes `store.getNextPlayType` return `'Run'`.
    - `store.applyNextPlayType({ next_type: 'Pass' })` makes `store.getNextPlayType` return `'Pass'` (D-10 tolerance for the `/game/nexttype` response shape).
    - `store.applyNextPlayType(null)` and `store.applyNextPlayType(undefined)` make `store.getNextPlayType` return `null`.
    - `store.applyNextPlayType(42)` makes `store.getNextPlayType` return `null` (non-string, non-`{next_type}` input normalizes to null rather than storing garbage).
    - Calling `store.applyNextPlayType('Run')` twice leaves `store.getNextPlayType` as `'Run'`.
    - `fetchPlayTypes` success still sets `getPlayTypes` to `['Run', 'Pass']` and `getNextPlayType` to `'Run'`.
    - `fetchPlayTypes` on an axios rejection still leaves `getPlayTypes` as `[]` and `getNextPlayType` as `null`.
    - After a successful `setPlayType('Pass')`, `store.gameMsg` is unchanged in behavior AND `store.getNextPlayType` is now `'Pass'` (the new additive optimistic write).
  </behavior>
  <action>
**Decision on Finding 2 (`fetchPlayTypes` writes two refs):** `applyNextPlayType(type)` owns ONLY the
`nextPlayType` ref, per D-10's scope. `playTypes` keeps its existing inline assignments in
`fetchPlayTypes` — it has a single write site already, so extracting it would add an action with no
second caller. Record this decision in a one-line comment above `applyNextPlayType`.

**Decision on Finding 1 (`setPlayType` does not currently write `nextPlayType`):** route it through
`applyNextPlayType` anyway. This is an **additive optimistic local write**, not a mechanical
extraction, and it is the correct reading of D-06 ("exactly one write site per field"). It is safe
because the existing `setPlayType` test asserts only `gameMsg`. Record this as a behavior change in
the plan SUMMARY.

In `src/stores/gameStore.js` add `applyNextPlayType(type)` with a JSDoc first line marking it
**local-apply only — does not POST** (D-07). Normalization rules, in order: if `type` is a non-empty
string, store it; else if `type` is a non-null object with a string `next_type`, store `type.next_type`;
else store `null`. Preserve the server-shaped `snake_case` field name `next_type` — do not rename it.

Refactor the three call sites (D-06):
- `fetchPlayTypes` success path: keep `playTypes.value = response.data.allowed_types || []` inline; replace `nextPlayType.value = response.data.next_type || null` with `applyNextPlayType(response.data.next_type)`.
- `fetchPlayTypes` error path: keep `playTypes.value = []` inline; replace `nextPlayType.value = null` with `applyNextPlayType(null)`. The error-reset behavior must be preserved exactly.
- `setPlayType` success path: after setting `gameMsg`, add `applyNextPlayType(playType)` as an optimistic local write.

Add `applyNextPlayType` to the public `return { ... }` surface inside the `// Local-apply actions (do not POST)` group.

In `src/stores/gameStore.test.js` add a `describe('applyNextPlayType (local-apply, no POST)')` block covering the direct cases, and extend the existing `setPlayType` test (or add a sibling test) asserting `store.getNextPlayType` is `'Pass'` after a successful `setPlayType('Pass')`.

Formatting: no semicolons, single quotes, 2-space indent, 100-col, no trailing commas.
  </action>
  <acceptance_criteria>
    - `src/stores/gameStore.js` contains `function applyNextPlayType(type)` and lists `applyNextPlayType` in the public `return` object.
    - `nextPlayType` is assigned in exactly one place: `grep -c 'nextPlayType.value =' src/stores/gameStore.js` returns 1.
    - `src/stores/gameStore.js` contains `applyNextPlayType(playType)` inside `setPlayType`.
    - The `fetchPlayTypes` error reset is preserved: `grep -c 'playTypes.value = \[\]' src/stores/gameStore.js` returns 1 and `grep -c 'applyNextPlayType(null)' src/stores/gameStore.js` returns 1.
    - `src/stores/gameStore.test.js` contains `describe('applyNextPlayType`.
    - `npx vitest run src/stores/gameStore.test.js` exits 0 with the pre-existing `fetchPlayTypes` and `setPlayType` cases still passing.
    - `npm test` exits 0.
    - `npx prettier --check src/stores/gameStore.js src/stores/gameStore.test.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm test && npx prettier --check src/stores/gameStore.js src/stores/gameStore.test.js</automated>
  </verify>
  <done>`nextPlayType` has exactly one write site; REST and WS share it; the `fetchPlayTypes` error reset is intact and `setPlayType` gained a documented optimistic local write.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the NextPlayTypeSet branch and assert full 5-variant coverage</name>
  <files>src/game/gameEventDispatch.test.js, src/game/gameEventDispatch.js</files>
  <read_first>
    - `src/game/gameEventDispatch.js` — the existing four branches and the shared non-null-object guard; match their style exactly.
    - `src/game/gameEventDispatch.test.js` — the `makeFakeStore()` helper and the established assertion idioms.
    - `src/game/gameEvents.js` — the canonical `KNOWN_VARIANTS` set (5 entries) that branch coverage must match exactly.
    - `.planning/phases/08-event-dispatch-glue/08-PATTERNS.md` — "loop-over-all-5-variants pattern", "table-driven per-variant / malformed cases", "Rejection logging (never throw)".
    - `.planning/phases/08-event-dispatch-glue/08-CONTEXT.md` — D-03, D-04, D-08, D-10.
  </read_first>
  <behavior>
    - `dispatchEvent(store, { event: 'NextPlayTypeSet', data: 'Run' })` records exactly one `applyNextPlayType` call with `'Run'` and returns `true`.
    - `dispatchEvent(store, { event: 'NextPlayTypeSet', data: { next_type: 'Pass' } })` records one `applyNextPlayType` call with the `{ next_type: 'Pass' }` object and returns `true`.
    - `data` of `null`, `undefined`, `42`, `''`, or an object with no `next_type` returns `false`, logs once, and leaves `store.calls` at length 0.
    - A completeness test loops the 5 variants from a locally-declared list and asserts each one records at least one store call with a well-formed payload — i.e. no variant falls into the unknown-tag default branch.
    - An unknown tag such as `'Nope'` returns `false`, logs a message containing `'Nope'`, and leaves `store.calls` at length 0.
    - Malformed envelopes (`null`, `undefined`, `42`, `'string'`, `{}`, `{ event: 123, data: {} }`) each return `false` and never throw.
  </behavior>
  <action>
First extend `src/game/gameEventDispatch.test.js` with a `describe('dispatchEvent — NextPlayTypeSet')` block and a `describe('dispatchEvent — variant coverage and defensive branches')` block covering every case in `<behavior>`, reusing the existing `makeFakeStore()`. Use the table-driven `for (const [label, input] of cases)` idiom for the malformed-envelope matrix. Run to confirm RED on the NextPlayTypeSet cases.

Then add the `'NextPlayTypeSet'` branch to `dispatchEvent` in `src/game/gameEventDispatch.js`. Accept `data` when it is a non-empty string, OR a non-null object whose `next_type` is a non-empty string (D-10). Anything else logs `` `dispatchEvent: ignoring NextPlayTypeSet — data is not a play type` `` and returns `false` with zero store calls. On success call `store.applyNextPlayType(data)` — pass `data` through unchanged and let the store action normalize it, so normalization has exactly one implementation. Return `true`.

Update the module header JSDoc to state that the router covers exactly the 5 `KNOWN_VARIANTS` from `gameEvents.js` and that the two lists must stay in lockstep.

Keep the `default` branch as the unknown-tag reject. Do not add a sixth branch.

Formatting: no semicolons, single quotes, 2-space indent, 100-col, no trailing commas.
  </action>
  <acceptance_criteria>
    - `src/game/gameEventDispatch.js` contains `'NextPlayTypeSet'` and `store.applyNextPlayType(`.
    - Branch count matches the canonical variant set: `grep -c "case '" src/game/gameEventDispatch.js` returns 5.
    - The module still has zero imports: `grep -c '^import ' src/game/gameEventDispatch.js` returns 0.
    - No network/async primitives: `grep -c 'axios\|fetch(\|await \|async ' src/game/gameEventDispatch.js` returns 0.
    - No POSTing action reachable: `grep -c 'store\.set' src/game/gameEventDispatch.js` returns 0.
    - No payload dumping: `grep -c 'JSON.stringify' src/game/gameEventDispatch.js` returns 0.
    - `src/game/gameEventDispatch.test.js` contains `describe('dispatchEvent — variant coverage and defensive branches'`.
    - `npx vitest run src/game/gameEventDispatch.test.js` exits 0.
    - `npm test` exits 0.
    - `npx prettier --check src/game/gameEventDispatch.js src/game/gameEventDispatch.test.js` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm test && npx prettier --check src/game/gameEventDispatch.js src/game/gameEventDispatch.test.js</automated>
  </verify>
  <done>All 5 EVT-02 variants dispatch to their correct store action against a zero-mock fake store, with an explicit completeness assertion and full defensive-branch coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Discharge the D-13 lineup-flow regression risk</name>
  <files>src/stores/gameStore.test.js, src/stores/gameStore.js</files>
  <read_first>
    - `src/stores/gameStore.js` — `applyPlayResult` (the D-13 reset added in plan 08-01), `fetchPlayResult`, `fetchGameData`, `runPlay`.
    - `src/components/GameLayout.vue` — the two `gamesStore.fetchGameData(...)` call sites and the `lineupSubmitted` usage in the template.
    - `src/components/PlayLineup.vue` — the `lineupSubmitted` usages in the template.
    - `src/stores/gameStore.test.js` — the `runPlay` test that already asserts `lineupSubmitted === false`.
    - `.planning/phases/08-event-dispatch-glue/08-CONTEXT.md` — D-13, D-14 and the `<specifics>` "Redundancy to verify" note.
    - `.planning/phases/08-event-dispatch-glue/08-PATTERNS.md` — the "D-13 regression check the planner must schedule" note.
  </read_first>
  <action>
Confirm, by reading the two call sites, that `fetchGameData` is reachable from exactly two places in
`GameLayout.vue`: the initial mount/refresh handler and the post-`runPlay` refresh. Neither can fire
while a user-submitted lineup is still pending, because `runPlay()` already sets
`lineupSubmitted = false` before `fetchGameData(false)` runs. Record this finding verbatim in the
plan SUMMARY as the discharge of the `<specifics>` risk.

Add a `describe('D-13 lineupSubmitted flow (REST path)')` block to `src/stores/gameStore.test.js`
proving the reset is benign and correctly gated:
- `runPlay()` followed by `fetchGameData(false)` with a NEW play leaves `lineupSubmitted === false` — the intended flow collapse, identical to today's behavior.
- With `lineupSubmitted` set to `true`, a `fetchGameData(false)` whose `fetchPlayResult` returns a DUPLICATE or STALE `play_counter` leaves `lineupSubmitted === true` — the D-14 zero-op guarantee, which is the actual mitigation for this risk.
- With `lineupSubmitted` set to `true`, a `fetchGameData(false)` whose play has no `new_state` leaves `lineupSubmitted === true`.
Mock axios per the existing file preamble (`vi.mock('axios')`, `setActivePinia(createPinia())`), using `axios.get.mockResolvedValueOnce` sequenced for the `fetchGame` → `fetchPlayTypes` → `fetchPlayResult` chain inside `fetchGameData`.

**Fallback, only if a test in this block genuinely fails and cannot be made to pass without changing
behavior:** move the `lineupSubmitted.value = false` reset out of `applyPlayResult` and into a
dispatch-only path — i.e. have `dispatchEvent`'s `PlayRun` branch call
`store.setLineupSubmitted(false)` when `applyPlayResult` returned `true`, and remove the reset from
the store action. Document the switch and its rationale in the SUMMARY. Do NOT take this fallback
pre-emptively.
  </action>
  <acceptance_criteria>
    - `src/stores/gameStore.test.js` contains `describe('D-13 lineupSubmitted flow (REST path)'` with at least three `it(` cases.
    - `npx vitest run src/stores/gameStore.test.js` exits 0.
    - `npm test` exits 0.
    - `npm run lint` exits 0.
    - The SUMMARY records the `fetchGameData` call-site audit result and states explicitly whether the fallback was taken.
  </acceptance_criteria>
  <verify>
    <automated>npm test && npm run lint</automated>
  </verify>
  <done>The recorded D-13 redundancy risk is discharged with tests proving the reset fires only on a genuine reducer advance, or the documented dispatch-only fallback is in place.</done>
</task>

</tasks>

<threat_model>
Schema gate: this phase touches **no database schema**. `workflow.schema_push_detection` is a no-op here.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebSocket server → `dispatchEvent` | Untrusted remote JSON; per-variant `data` shapes are assumptions until Phase 11 (D-11). |
| `dispatchEvent` → `gameStore.nextPlayType` | Untrusted remote data becomes UI-driving state here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-13 | Tampering | `dispatchEvent` NextPlayTypeSet branch | high | mitigate | `data` is accepted only as a non-empty string or an object with a string `next_type`; anything else is logged and ignored with zero store calls (D-08, D-10). |
| T-08-14 | Tampering | `applyNextPlayType` normalization | medium | mitigate | Normalization reads only the single named field `next_type` and stores a primitive string or `null` — no object spread or `Object.assign` of attacker-controlled keys into store state, so no prototype-pollution merge surface. |
| T-08-15 | Elevation of privilege | dispatch → `setPlayType` | high | mitigate | The branch calls `applyNextPlayType` (local-only), never `setPlayType` (POSTs to `/game/nexttype`). Asserted by `grep -c 'store\.set'` returning 0 (D-05, D-06). |
| T-08-16 | Spoofing | unknown / forged event tags | medium | mitigate | The `default` branch rejects any tag outside the 5 `KNOWN_VARIANTS`; branch count is asserted at exactly 5, so no undeclared tag can reach a store action (D-04). |
| T-08-17 | Information disclosure | `console.error` in the new branch | medium | mitigate | Message interpolates only the event tag; asserted by the no-payload-leak test. |
| T-08-18 | Denial of service | unknown-tag flood | low | accept | One `console.error` per event on a client-side SPA; disproportionate to mitigate at ASVS L1. |

Generic OWASP web-application concerns (injection, XSS, CSRF, auth) are out of scope for this
change and are covered by the project's baseline canon — breadcrumb noted, not re-litigated here.

ASVS level 1; blocking severity: high. All high-severity threats are dispositioned `mitigate`.
</threat_model>

<verification>
- `npm test` exits 0 (full unit/component suite).
- `npm run lint` exits 0.
- `npx prettier --check src/game/gameEventDispatch.js src/game/gameEventDispatch.test.js src/stores/gameStore.js src/stores/gameStore.test.js` exits 0.
- `grep -c "case '" src/game/gameEventDispatch.js` returns 5, matching the 5 entries of `KNOWN_VARIANTS` in `src/game/gameEvents.js`.
</verification>

<success_criteria>
- `dispatchEvent(store, event)` routes all 5 variants to the correct store action (ROADMAP #1, EVT-02 — complete).
- `PlayRun` applies the full `GameState` at `data.new_state` (ROADMAP #2, EVT-03 — completed in 08-01, re-verified green here).
- All `GameState`-bearing variants land through `applyGameState` (ROADMAP #3 — complete).
- Per-variant tests against a fake store verify correct extraction and dispatch for all 5 variants (ROADMAP #4 — complete, folding into RTT-01/RTT-02 coverage).
- The D-13 redundancy risk from `<specifics>` is discharged with tests or the documented fallback.
</success_criteria>

<output>
Create `.planning/phases/08-event-dispatch-glue/08-03-SUMMARY.md` when done.
</output>
