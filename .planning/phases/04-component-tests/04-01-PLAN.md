---
phase: 04-component-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/PlayResult.test.js
autonomous: true
requirements: [CMP-01, CMP-03]
must_haves:
  truths:
    - "PlayResult.vue renders the correct outcome color/icon/label for every classifyOutcome branch (favorable turnover, unfavorable turnover, favorable positive-yardage, favorable zero/negative-yardage) when mounted with real Pinia + real Vuetify (D-01)"
    - "PlayResult.vue renders the 'No play result available' empty-state card (not the v-alert path) when the store has no playResult yet (D-02)"
    - "All mounts use the real createTestVuetify() plugin instance with zero component stubs (D-07) and produce no unresolved-component or missing-global console errors (CMP-03)"
  artifacts:
    - src/components/PlayResult.test.js
  key_links:
    - "Test seeds a real Pinia gameStore (via playResults state, indirectly reachable through fetchPlayResult mock or direct store mutation) and teamsStore.managedTeam, not vi.mock('@/stores/gameStore') (D-05)"
    - "Outcome expectations per branch are lifted from src/game/playOutcome.test.js's classifyOutcome table, not re-derived (D-01)"
---

<objective>
Prove `PlayResult.vue` renders the correct outcome color/icon/label from real store state across all `classifyOutcome` branches, plus the empty-state guard, mounted with the real Vuetify plugin and no stubs.

Purpose: Lock down the presentation component highest-priority test target (CMP-01) so a broken outcome-color/icon binding, or an accidentally-removed empty-state guard, becomes a failing test.
Output: `src/components/PlayResult.test.js` — a runnable, passing component test suite.
</objective>

<execution_context>
@/mnt/c/Users/taragin/OneDrive - Hewlett Packard Enterprise/Config/.config/opencode/gsd-core/workflows/execute-plan.md
@/mnt/c/Users/taragin/OneDrive - Hewlett Packard Enterprise/Config/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-component-tests/04-CONTEXT.md
@.planning/phases/03-store-unit-tests/03-CONTEXT.md
@src/components/PlayResult.vue
@src/game/playOutcome.js
@src/game/playOutcome.test.js
@test/setup.js
@test/factories/gameState.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: PlayResult.vue component test suite (CMP-01, CMP-03)</name>
  <files>src/components/PlayResult.test.js</files>
  <read_first>
    - src/components/PlayResult.vue (full file — note the top-level v-if="playResult" / v-else guard, the outcome/outcomeColor/outcomeIcon/outcomeLabel/resultYards computeds, and that playResult reads gameStore.getPlayResult while classifyOutcome's favorable option is derived from managedTeamHadPossession(playResult, teamsStore.managedTeam))
    - src/stores/gameStore.js (getPlayResult computed reads the LAST entry of the internal playResults ref array — there is no public setter, so seed it by pushing a well-formed play object onto the store's exposed state via the store instance directly, e.g. store.$patch or by driving fetchPlayResult() with a mocked axios.get response shaped like { data: [play] } per fetchPlayResult's Array.isArray(response.data) handling and its play_counter gating in updateGameStateFromPlayResult)
    - src/stores/teamStore.js (managedTeam ref, default 'Home' — read via storeToRefs or store.managedTeam)
    - src/game/playOutcome.js and src/game/playOutcome.test.js (classifyOutcome's color/icon/label are pure derivations of result_type + result + favorable — reuse the exact expected values from playOutcome.test.js's classifyOutcome/outcomeColor/outcomeIcon/outcomeLabel assertions as the source of truth for each branch: e.g. Complete/result:8/favorable:true -> success/mdi-arrow-up-bold/Complete; TurnOver/favorable:true -> error/mdi-alert-octagon/Turnover; TurnOver/favorable:false -> success/mdi-alert-octagon/Turnover; Incomplete/result:0/favorable:true -> mdi-football icon)
    - test/setup.js (createTestVuetify — import and pass as global.plugins on every mount, per D-07, no stubs)
    - test/factories/gameState.js (buildGameState for the new_state shape a play result must carry: home_score, away_score, quarter, time_remaining, possession, yard_line, first_down_target, last_status, down, plus play_counter which fetchPlayResult/updateGameStateFromPlayResult additionally require)
    - .planning/codebase/CONVENTIONS.md (Prettier: no semicolons, single quotes, 2-space indent, no trailing commas)
  </read_first>
  <action>
    Create `src/components/PlayResult.test.js`. Import `mount` from `@vue/test-utils`, `createPinia`/`setActivePinia` from `pinia`, `vi`/`describe`/`it`/`expect`/`beforeEach` from `vitest`, `axios`, `PlayResult` from `./PlayResult.vue`, `useGameStore` from `@/stores/gameStore`, `useTeamsStore` from `@/stores/teamStore`, `createTestVuetify` from `../../test/setup.js`, and `buildGameState` from `../../test/factories/gameState.js`.
    Add `vi.mock('axios')` at module scope and a `beforeEach` that calls `setActivePinia(createPinia())` and `vi.clearAllMocks()`.
    Write a helper `mountPlayResult()` that instantiates a fresh Vuetify instance via `createTestVuetify()` and returns `mount(PlayResult, { global: { plugins: [pinia_instance, vuetify_instance] } })` — obtain the active pinia via `createPinia()` passed explicitly into mount's global.plugins alongside the Vuetify instance (do not rely on implicit global pinia binding for the component under test; explicit is more reliable with test-utils). Do not use any `stubs` option per D-07.
    Add a helper `seedPlayResult(overrides = {})` that: (1) mocks `axios.get` to resolve with `{ data: [{ result: { result_type, result, time: 5, details: [], mechanic: null, cards: null }, new_state: { ...buildGameState(), play_counter: 1 } }] }` shaped per the `overrides.result` and `overrides.newState` passed in, then (2) calls `await gameStore.fetchPlayResult()` on the store instance obtained via `useGameStore()` inside the currently-active pinia so `getPlayResult` becomes populated (per D-05 — exercise real store wiring, not direct field assignment).
    Write test cases, per D-01, covering four classifyOutcome branches sourced from playOutcome.test.js's known-good expectations: (1) favorable turnover (result_type: 'TurnOver', result: 0, managedTeam matching new_state.possession) asserts alert renders with error color, mdi-alert-octagon icon, and 'Turnover' label text; (2) unfavorable turnover (possession mismatched so managedTeamHadPossession is false) asserts success color with the same icon/label; (3) favorable positive-yardage completion (result_type: 'Complete', result: 8) asserts success color, mdi-arrow-up-bold icon, 'Complete' label, and the yards value '8' rendered; (4) zero/negative-yardage non-turnover (result_type: 'Incomplete', result: 0) asserts mdi-football icon. For each, assert on the rendered DOM: the alert's applied color/icon can be read via the mounted wrapper's `find('.v-alert')` classes/attributes (Vuetify applies `text-{color}` classes and an `.mdi-{icon}` icon element) or, more robustly, assert the component's exposed text content (`wrapper.text()`) contains the expected label and yards, and separately assert `wrapper.find('.v-alert').classes()` contains a class referencing the expected color/icon token — inspect actual rendered markup during implementation and assert against what Vuetify actually emits, not assumed class names.
    Add one test for D-02: with no play result seeded (fresh store, no fetchPlayResult call), mount the component and assert `wrapper.text()` contains 'No play result available' and that no `.v-alert` element exists in the mounted output (proving the v-else empty-state branch rendered, not the v-if branch).
    Add one assertion (can be folded into an existing test or its own) proving CMP-03: mounting completes without throwing and without any `[Vue warn]` unresolved-component console output — spy on `console.warn`/`console.error` around one mount and assert no calls matching `/Failed to resolve component/` or `/inject\(\) can not be used/` occurred.
    Follow Prettier conventions exactly: no semicolons, single quotes, 2-space indent, print width 100, no trailing commas.
  </action>
  <verify>
    <automated>npx vitest run src/components/PlayResult.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/PlayResult.test.js` exits 0
    - Test file contains at least 4 distinct classifyOutcome-branch cases (favorable turnover, unfavorable turnover, favorable positive yardage, zero/negative yardage) plus one empty-state case
    - Every mount call passes `createTestVuetify()`'s plugin instance via `global.plugins`, with no `stubs` option present anywhere in the file
    - No test seeds state via `vi.mock('@/stores/gameStore')` or direct field mutation bypassing the store's real actions/computeds
  </acceptance_criteria>
  <done>PlayResult.test.js passes green, covering all outcome branches and the empty state, mounted with the real Vuetify plugin and real Pinia store wiring — no stubs, no unresolved-component warnings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process ↔ mocked axios | Test-only boundary; no real network. Mocking is scoped to the test file via `vi.mock('axios')`, not a source change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | Test fixtures reused from Phase 3 factories | low | accept | Factories contain only synthetic seed data (no real credentials/PII); no mitigation needed. |
| T-04-02 | Tampering | No new npm/pip/cargo packages installed this plan | low | accept | Plan only adds a test file consuming already-installed `@vue/test-utils`, `pinia`, `vitest`, `vuetify` — no package legitimacy gate required. |
</threat_model>

<verification>
- `npx vitest run src/components/PlayResult.test.js` exits 0
- `npm run lint` reports no new errors in `src/components/PlayResult.test.js`
</verification>

<success_criteria>
CMP-01 and CMP-03 are satisfied for `PlayResult.vue`: all classifyOutcome branches (per D-01) and the empty-state guard (per D-02) render correctly from real store state under the real Vuetify plugin, with zero stubbing and zero unresolved-component warnings.
</success_criteria>

<output>
Create `.planning/phases/04-component-tests/04-01-SUMMARY.md` when done
</output>
