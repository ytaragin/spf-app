---
phase: 04-component-tests
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/PlayTypeSelector.test.js
autonomous: true
requirements: [CMP-02, CMP-03]
must_haves:
  truths:
    - "PlayTypeSelector.vue fetches play types on mount via gameStore.fetchPlayTypes() (mocked axios.get) and renders a v-btn for each returned play type, seeded through a real Pinia store (D-06)"
    - "Clicking a play-type button calls gameStore.setPlayType() with the selected value and triggers the mocked axios.post call (D-06)"
    - "The component mounts cleanly with the real createTestVuetify() plugin instance and zero component stubs (D-07), with no unresolved-component or missing-global console errors (CMP-03)"
  artifacts:
    - src/components/PlayTypeSelector.test.js
  key_links:
    - "Test seeds a real Pinia gameStore (setActivePinia(createPinia())) and mocks axios at the boundary — not vi.mock('@/stores/gameStore') (D-05)"
    - "axios.get is mocked for the onMounted fetchPlayTypes() call and axios.post is mocked for the click-triggered setPlayType() call, matching Phase 3's gameStore.test.js mocking pattern (D-06)"
---

<objective>
Prove `PlayTypeSelector.vue` — the interaction/flow component picked per D-03 — correctly fetches play types on mount and dispatches `setPlayType` on user interaction, seeded through a real Pinia store with mocked axios, mounted with the real Vuetify plugin.

Purpose: Satisfy CMP-02 (an interaction/flow component tested with seeded Pinia state) using a small, clear, store-driven surface rather than the heavier GameLayout orchestrator (deferred to Phase 5 per D-04).
Output: `src/components/PlayTypeSelector.test.js` — a runnable, passing component test suite.
</objective>

<execution_context>
@/mnt/c/Users/taragin/OneDrive - Hewlett Packard Enterprise/Config/.config/opencode/gsd-core/workflows/execute-plan.md
@/mnt/c/Users/taragin/OneDrive - Hewlett Packard Enterprise/Config/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-component-tests/04-CONTEXT.md
@.planning/phases/03-store-unit-tests/03-CONTEXT.md
@src/components/PlayTypeSelector.vue
@src/stores/gameStore.js
@src/stores/gameStore.test.js
@test/setup.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: PlayTypeSelector.vue component test suite (CMP-02, CMP-03)</name>
  <files>src/components/PlayTypeSelector.test.js</files>
  <read_first>
    - src/components/PlayTypeSelector.vue (full file — note onMounted calls gameStore.fetchPlayTypes() then updateSelectedPlayType(); playTypes computed maps gameStore.getPlayTypes with a fallback to two hardcoded defaults when empty/absent; nextPlayType computed reads gameStore.getNextPlayType; selectPlayType(playType) sets selectedPlayType.value and calls gameStore.setPlayType(playType); a watch(playTypes, ...) auto-selects when exactly one option remains)
    - src/stores/gameStore.js (fetchPlayTypes: axios.get(`${baseUrl}/game/nexttype`) resolving { allowed_types, next_type } sets playTypes/nextPlayType refs, exposed via getPlayTypes/getNextPlayType computeds; setPlayType: axios.post(`${baseUrl}/game/nexttype`, playType, { headers: {'Content-Type':'text/plain'} }) sets gameMsg on success)
    - src/stores/gameStore.test.js (fetchPlayTypes test block — axios.get.mockResolvedValueOnce({ data: { allowed_types: [...], next_type: ... } }) is the exact shape to mock; note vi.mock('axios') at module scope + beforeEach setActivePinia(createPinia())/vi.clearAllMocks())
    - test/setup.js (createTestVuetify — pass as global.plugins on every mount, per D-07, no stubs)
    - .planning/codebase/CONVENTIONS.md (Prettier: no semicolons, single quotes, 2-space indent, no trailing commas)
  </read_first>
  <action>
    Create `src/components/PlayTypeSelector.test.js`. Import `mount` from `@vue/test-utils`, `createPinia`/`setActivePinia` from `pinia`, `vi`/`describe`/`it`/`expect`/`beforeEach` from `vitest`, `axios`, `PlayTypeSelector` from `./PlayTypeSelector.vue`, `useGameStore` from `@/stores/gameStore`, and `createTestVuetify` from `../../test/setup.js`.
    Add `vi.mock('axios')` at module scope and a `beforeEach` that calls `setActivePinia(createPinia())` and `vi.clearAllMocks()`.
    Before each mount, per D-06, set `axios.get.mockResolvedValue({ data: { allowed_types: ['Run', 'Pass'], next_type: 'Run' } })` (use `mockResolvedValue`, not `mockResolvedValueOnce`, since `onMounted` may trigger the fetch during the mount lifecycle and any subsequent watcher-driven read should still resolve consistently) and `axios.post.mockResolvedValue({ data: 'play type set' })` so both the onMounted fetch and any click-triggered post resolve realistically.
    Write a helper `mountSelector()` that mounts `PlayTypeSelector` with `global: { plugins: [createPinia() /* or the active instance from setActivePinia */, createTestVuetify()] }` — no `stubs` option, per D-07 — and awaits `flushPromises` (from `@vue/test-utils`) after mount so the onMounted fetchPlayTypes() microtask resolves and playTypes/nextPlayType propagate through the store's computeds before assertions run.
    Add a test asserting: after mount + flush, `axios.get` was called with a URL ending in `/game/nexttype`, and the rendered output contains a `v-btn` (or `.v-btn-toggle` child button) for each of 'Run' and 'Pass' (assert via `wrapper.findAll('button')` text content, or `wrapper.text()` containing both labels).
    Add a test asserting the `nextPlayType` chip renders: after mount + flush, `wrapper.text()` contains 'Run' inside the "Next Play Type" chip region (assert against the actual rendered chip element/class, inspecting real markup during implementation).
    Add a test for the click-interaction half of D-06: after mount + flush, find the button corresponding to 'Pass', trigger a click, `await flushPromises` again, then assert `axios.post` was called with a URL ending in `/game/nexttype` and a body/payload equal to `'Pass'`, and that the store's `gameMsg` reflects the mocked response (drive this through the same `useGameStore()` instance bound to the active pinia used during mount, per D-05 — do not assert against an unrelated store instance).
    Add one assertion proving CMP-03 for this component: spy on `console.warn`/`console.error` around one mount and assert no calls matching `/Failed to resolve component/` or `/inject\(\) can not be used/` occurred.
    Follow Prettier conventions exactly: no semicolons, single quotes, 2-space indent, print width 100, no trailing commas.
  </action>
  <verify>
    <automated>npx vitest run src/components/PlayTypeSelector.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/PlayTypeSelector.test.js` exits 0
    - Test file mocks both `axios.get` (onMounted fetch) and `axios.post` (click-triggered setPlayType) per D-06
    - At least one test asserts a click on a play-type button results in an `axios.post` call carrying the selected value
    - Every mount call passes `createTestVuetify()`'s plugin instance via `global.plugins`, with no `stubs` option present anywhere in the file
    - No test seeds state via `vi.mock('@/stores/gameStore')` or direct field mutation bypassing the store's real actions/computeds
  </acceptance_criteria>
  <done>PlayTypeSelector.test.js passes green, covering the onMounted fetch, rendered play-type buttons, click-triggered setPlayType dispatch, and the nextPlayType chip — mounted with the real Vuetify plugin and real Pinia store wiring, no stubs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process ↔ mocked axios | Test-only boundary; no real network. Mocking is scoped to the test file via `vi.mock('axios')`, not a source change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Information Disclosure | Test fixtures (play type strings) | low | accept | Synthetic values only ('Run'/'Pass'); no real payload risk. |
| T-04-04 | Tampering | No new npm/pip/cargo packages installed this plan | low | accept | Plan only adds a test file consuming already-installed `@vue/test-utils`, `pinia`, `vitest`, `vuetify` — no package legitimacy gate required. |
</threat_model>

<verification>
- `npx vitest run src/components/PlayTypeSelector.test.js` exits 0
- `npm run lint` reports no new errors in `src/components/PlayTypeSelector.test.js`
</verification>

<success_criteria>
CMP-02 and CMP-03 are satisfied for `PlayTypeSelector.vue`: onMounted fetch, rendered options, and click-triggered store dispatch all work against a real Pinia store with mocked axios, under the real Vuetify plugin with zero stubbing.
</success_criteria>

<output>
Create `.planning/phases/04-component-tests/04-02-SUMMARY.md` when done
</output>
